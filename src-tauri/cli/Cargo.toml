[package]
name = "muniment-cli"
version = "0.0.1"
edition = "2021"

[dependencies]
muniment-attach = { path = "../attach", default-features = false, features = ["client"] }

[target.'cfg(unix)'.dependencies]
libc = "0.2"

[dev-dependencies]
serde_json = "1"
