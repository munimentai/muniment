[package]
name = "muniment-attach"
version = "0.0.1"
edition = "2021"

[features]
default = []
client = []

[lib]
path = "src/lib.rs"

[dependencies]
chrono = { version = "0.4", default-features = false }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
uuid = "1"
