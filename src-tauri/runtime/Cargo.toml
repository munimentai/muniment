[package]
name = "muniment-runtime"
version = "0.0.1"
edition = "2021"

[dependencies]
muniment-attach = { path = "../attach", features = ["client"] }
muniment-core = { path = "../core" }

[dev-dependencies]
serde_json = "1"
