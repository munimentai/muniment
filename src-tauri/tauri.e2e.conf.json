{
  "$schema": "https://schema.tauri.app/config/2",
  "app": {
    "security": {
      "capabilities": [
        "default",
        "launcher",
        {
          "identifier": "e2e-webdriver",
          "description": "Embedded WebDriver access for E2E builds.",
          "windows": ["main", "launcher"],
          "permissions": ["wdio-webdriver:default"]
        }
      ]
    }
  }
}
