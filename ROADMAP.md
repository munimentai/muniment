# muniment-desktop — ROADMAP

Phases mirror harness-spec §9. Muniment-cloud Phase 1 native auth and the cloud prerequisites for desktop chat are live as of 2026-07-11; client work that uses them must exercise the real contracts and must not introduce mocked production paths.

## M0 — Scaffold (done 2026-07-09)
- DONE — Tauri v2 desktop shell, specs, mockups, design reference, Rust/frontend test harnesses, and Linux/Windows/macOS CI gates.

## Phase 2 — Client core (§9 items 8–11)

### 8. Shell, auth, and entitlements
- DONE — Svelte 5/Vite shell, design tokens, signed-out/signed-in states, native installation-bound authorization, keychain persistence, refresh/session/revocation flows, live entitlement projection, access-device states, and server-unreachable recovery.
- The production contract is `/v1/auth/native/*`; generic OIDC remains test groundwork and is not the production handshake.

### 9. Pi sidecar and cloud chat
- DONE — verified Pi runtime acquisition/supervision, signed-in streamed chat, steering/follow-up, durable tool effects, provenance/receipts, inline tool cards, extension UI, permission-gate replay/answers, and safe interrupted-session resume.
- OBSERVED 2026-07-27 — the desktop cannot answer a Pi permission gate, so the "answers" above cover the companion lanes and journal replay alone. `parse_extension_ui_request` decodes the request. `chat.rs` journals `permission.requested` and projects the typed gate into `pendingPermission`. `ExtensionUiResponse` has no production caller: a grep over `src-tauri/` finds it only in `core/tests/pi_chat.rs`. No Tauri command answers a gate, and the frontend reads `pendingPermission` nowhere. A gated run therefore parks in phase `pending-permission` behind the ordinary thinking spinner, and only Cancel ends it. `src/lib/chat-state.js:72` records the gap in a comment. `02-desktop-app.md` §8 specifies the surface: an inline mono ask card naming the exact path or command.
- DONE 2026-07-27 — the permission-gate core seam (MUNIDESK-577): `PiRpcTransport::send` writes one pre-correlated frame, and `PiRunAdapter::answer_extension_ui` validates the answer against its request through `ExtensionUiResponse` before sending it. Planner review 2026-07-27 confirmed the child-generation check on send and the validate-before-send order. The seam still has no production caller.
- DESIGN CALL 2026-07-27 (planner, gate answers) — the coordinate loop owns a run's journal sequence, so the desktop command hands its answer to that loop through the active-run state, the way `chat_cancel` hands over cancellation. The loop keeps the pending `ExtensionUiRequest`, sends the validated answer, and appends `permission.resolved` itself.
- DONE 2026-07-27 — that command landed (MUNIDESK-582): `chat_answer_permission` pushes a typed `ChatPermissionAnswer` onto the active run's `permission_answers` queue, and the coordinate loop drains it, validates each answer against the pending `ExtensionUiRequest`, sends it, and appends `permission.resolved`. Planner review 2026-07-28 confirmed the run-id guard on the queue write and the test that resolves only matching valid answers.
- DONE 2026-07-28 — that card landed (MUNIDESK-587): `src/lib/chat-state.js` carries `pendingPermission` through every projection, and the thread renders a mono card with the request title, the exact path or command, and the answer controls. `confirm` renders Allow and Deny, `select` renders one button per option plus Deny, and any other kind renders the title plus Deny alone. A parked run now announces `Waiting for your decision.` instead of the coarse generating string, and `test/probe/permission.html` is the fixture. Planner render 2026-07-28 of the built bundle at 1100×720 confirmed the card, its mono register, and both controls on screen.
- DONE 2026-07-28 — the text-entry half landed (MUNIDESK-594): an `input` gate renders a single-line field with the request placeholder, an `editor` gate renders a prefilled multi-line field, and both render Submit beside Deny. `test/probe/input.html` and `test/probe/editor.html` are the fixtures. Planner render 2026-07-28 of the built bundle at 1100×720 confirmed both cards on screen, each field labelled by the request title in the mono register.
- NOTED 2026-07-28 (planner) — two ask-card gaps stay parked. The gate payload carries `timeout`, and the card shows no remaining time. Design-spec §8 asks for `Allow once` and `Allow for this thread`, and the second needs a per-thread policy store that no contract defines yet. The card shipped in two consecutive waves, so it rests one wave before the next slice.
- MEASURED 2026-07-29 (planner code read) — the gate timeout already reaches the frontend. All four `PermissionRequest` kinds carry `timeout` (`src-tauri/core/src/journal/reducer.rs:179`), `PermissionGate` flattens the request, and `ChatPendingPermission` flattens it again (`src-tauri/src/chat.rs:109`). No file under `src/` reads `pendingPermission.timeout`. The missing piece is the anchor, not the plumbing. A live gate can anchor on arrival, but a gate restored from the journal cannot, because the desktop never carries the `permission.requested` recorded time to the frontend. The countdown therefore needs a design call on the anchor and on the copy at zero. It stays parked rather than becoming a UI slice.
- MEASURED 2026-07-30 (planner code read) — the second half of that design call is still unanswerable. `parse_extension_ui_request` (`src-tauri/core/src/sidecar/pi_chat.rs:287`) reads `timeout` and carries it, and no desktop code enforces it. Nothing cancels a gate, fails a run, or appends an event when the value elapses, so the copy at zero describes Pi's behavior rather than the desktop's. The countdown stays parked until the Pi contract states what Pi does when its own gate timeout expires.
- DONE 2026-07-28 — the keyboard commit landed (MUNIDESK-605): plain Enter commits the single-line `input` gate, the platform chord plus Enter commits the multi-line `editor` gate, and `npm run probe` now prints all five fixture URLs. Planner check 2026-07-28 confirmed the `permissionGateAction` helper beside `composerAction` in `src/lib/chat-state.js`, its Shift/Alt/composition rejections, and the two new gate tests. The timeout countdown and the §8 `Allow for this thread` policy stay parked, and the ask card rests again.

### 10. Local model sidecar
The 2026-07-29 cloud ingress ruling supersedes this section. These entries
record deleted runtime work.

- DONE — supervised local runtime, pinned model lifecycle, verified/cancellable acquisition, rollback, native adapters, and Tauri install/status/cancel commands.
- DONE 2026-07-22 — first-use onboarding shows required local-model acquisition progress, readiness, and redacted background-retry status while keeping folder setup fail-open and AI proposal generation fail-closed.
- DONE 2026-07-27 — ADR 0017 supersedes ADR 0003: the decision record now pins the artifact that actually ships (Qwen3.5-4B Q4_K_M GGUF, Apache-2.0, alias `muniment-required-qwen3.5-4b`, 262,144-token context) byte-for-byte against `RESIDENT_MODEL`, with a core guard test that fails if the ADR table or the descriptor is edited alone; ADR 0006's subject is now that artifact, and the sidecar/harness-spec Gemma alias and licence claims are corrected. The owner-decided swap to a muniment-built, muniment-hosted artifact is named as the pending follow-up rather than pinned with invented values.
- DONE 2026-07-27 — the owner-decided swap landed (MUNIDESK-571/572). The first-run required-model download now follows Hugging Face's regional Xet CDN redirect, because the initial-URL policy is split from the redirect policy, and `RESIDENT_MODEL` plus ADR 0017 pin muniment's own hosted Qwen3.5-4B Q4_K_M build with the initial URL constrained to the munimentai org.
- CANCELLED 2026-07-29 — the desktop ships no resident artifact notice.
- DONE 2026-07-30 — the removal finished (MUNIDESK-680). `src-tauri/core/src/llama.rs` and its four submodules are gone, together with `src/lib/model-acquisition-state.js`, seven core test binaries, and the routing-classifier golden fixture. ADR 0018 and ADR 0021 now carry superseded status, and ADR 0014 records a distribution decision whose code no longer ships. Planner check 2026-07-30 found no live `llama` reference under `src/` or `src-tauri/src/`, `cargo clippy -p muniment-core --all-targets` reported no warnings, and the core suite passed in full.

### 11. Attachments and local CAS
- DONE groundwork — pure-Rust content-addressed local store with atomic deduplication, constant-memory I/O/verification, stale-temp cleanup, journal reference accounting, retention, export, and compaction. Cloud file flow follows items 8d/9.

### Durable local run journal
- DECIDED — ADR 0002 makes a per-run append-only SQLite event journal authoritative; external effects are never silently re-executed and large bodies live in CAS.
- DONE — schema/envelope/atomic append, reducer/replay, Pi translation, deletion/collection/retention, deterministic export, crash-safe compaction, cursor-paginated run summaries, and authorized Linux companion `thread.list`/`thread.open` backed by redacted projections.
- RULE 2026-07-12 — Pre-launch schema work on the desktop's local journal is in scope for this lane. The owner's post-go-live restriction applies to the muniment-cloud deploy path, not this repository.
- OBSERVED 2026-07-28 (planner measurement, database lens) — `events_run_order` on `events(run_id, run_seq)` duplicates the index that `UNIQUE(run_id, run_seq)` already creates on the same columns in the same order. On a replica `events` table holding 160,000 rows, dropping it cut the file from 91.6 MB to 88.4 MB, and `EXPLAIN QUERY PLAN` for the run-ordered read still reported `SEARCH events USING INDEX sqlite_autoindex_events_2 (run_id=?)`. `events` is the fastest-growing table, so every append pays for a second identical b-tree entry. DONE 2026-07-28 — that drop landed (MUNIDESK-589) as a schema-v4 migration step. `thread_events_thread_order` and `run_threads_thread_run` duplicate their own UNIQUE constraints the same way, and they stay parked because `validate_thread_schema` asserts their exact SQL and two corruption tests tamper with them.
- Compaction copies the schema with `VACUUM INTO`, so an index change needs no compaction work.
- MEASURED 2026-07-31 (planner, read every SQLite call site in `src-tauri/core/src/journal/`, database lens) — app launch reads the whole journal twice, and both reads grow without bound. `validate_database` (`src-tauri/core/src/journal/mod.rs:2013`) runs `SELECT ... FROM events ORDER BY run_id,run_seq` with no `WHERE` and no `LIMIT`, deserializes each row, and re-serializes it through `canonical_envelope` to compare bytes. It then scans `thread_events` the same way and runs five more unbounded aggregates. `RunJournal::open` calls it unconditionally (`mod.rs:672`), and `ChatState::new` opens the journal once per launch (`src-tauri/src/chat.rs:539`). `reconcile_interrupted_runs` (`src/chat.rs:585`) then calls `run_event_types` (`mod.rs:1198`), a second unbounded ordered read of every event ever recorded, and issues one further query per non-terminal run. A journal holding 50,000 events therefore pays 50,000 JSON parses plus 50,000 re-serializations before the first frame, every launch, forever.
- NOT SELECTED 2026-07-31 (planner) — that pair needs a design call before it needs a ticket. `validate_database` exists to catch a tampered or corrupted journal, so bounding its scan weakens the guarantee it provides. The candidates are a bounded newest-events check at open with the full pass moved off the launch path, a full pass on a schedule, and a stored high-water mark that records how far validation has already reached. Naming one is the next slice, and it is a decision rather than a query rewrite.
- MEASURED 2026-07-31 (planner, read the open path again) — the launch cost is larger than that note records. `validate_database` opens with `PRAGMA integrity_check` (`src-tauri/core/src/journal/mod.rs:2007`), which walks every page of the file before the envelope pass starts. `RunJournal::open` (`mod.rs:638`) calls that function at `mod.rs:672` on every open. A launch therefore pays a full structural check, a full envelope re-serialization pass, and then the separate `run_event_types` read. ADR 0002 states the current rule in one sentence: "On open, SQLite integrity and envelope/sequence validation precede replay." SQLite documents `PRAGMA quick_check` as the cheaper structural check, and published operating guidance keeps a full integrity check off the interactive path.
- SELECTED 2026-07-31 (this wave) — that decision, as a dated amendment inside ADR 0002. The amendment names the launch-path read policy for the open-time check and for the interrupted-run reconcile. It states the guarantee each answer keeps and the one it drops, states when the full pass runs, states whether a schema step follows, and names the first implementation slice. It writes no runtime code.
- DONE 2026-07-31 — that amendment landed (MUNIDESK-720). On open the desktop runs `PRAGMA quick_check`. It then validates the 256 highest-`append_ordinal` envelopes in `events` and the 256 highest in `thread_events`. It also reads each represented stream's boundary predecessor. The amendment states what those bounded checks still catch and what they no longer catch. The full integrity, envelope, sequence, schema, identity, and aggregate pass runs after the first frame, and the desktop gates every external effect until it succeeds. `reconcile_interrupted_runs` reads candidate run IDs from a transactionally maintained recovery-state projection instead of reading every event type. Planner read 2026-07-31 confirmed the corruption response, the preserved fail-open classification, and the two named implementation slices.
- SEQUENCED 2026-07-31 (planner) — that first slice names three storage changes. It adds `append_ordinal` to `events`, adds the same column to `thread_events`, and adds the recovery-state projection with its own backfill. Three storage changes in one diff is more than one reviewable ticket carries, so the step splits by table and then by projection. The `events` column is the cheapest first slice. `validate_thread_schema` (`src-tauri/core/src/journal/mod.rs:2086`) asserts the exact stored SQL of `thread_events` and `run_threads`, and two corruption tests tamper with those objects. `VACUUM INTO` copies column values, so compaction needs no work either way.
- HELD 2026-07-31 (planner) — no schema slice is filed this wave, and the planner asks the owner for one ruling. The standing owner exclusion on self-initiated database migrations and the 2026-07-12 RULE above disagree about this repository's local journal. Migration steps 2, 3, and 4 all landed under that RULE. Every later slice in this lane waits on the answer, because the bounded open-time check and the recovery-state projection both need the schema step first.
- MEASURED 2026-07-31 (planner, same read) — three further query defects sit behind that pair. `apply_retention` (`src-tauri/core/src/journal/retention.rs:69`) loops over `run_ids()`, itself an unbounded `GROUP BY` over `events`, and calls `journal.events(&run_id)` once per run, so a journal with 20,000 historical runs makes 20,000 prepare-and-query round trips. No production caller reaches it yet. `thread_summaries_inner` (`src-tauri/core/src/journal/thread_summaries.rs:118`) executes its unbounded aggregation CTE twice whenever a cursor is present, once for the boundary check at `:132` and once for the page at `:149`, then runs up to two more per-thread lookups for each of 100 threads at `:183` and `:205`. `append_batch` (`mod.rs:1008` and `:1051`) prepares both of its statements inside the per-event loop, which costs 2N re-parses for a batch of N. The `thread_summaries` half no longer waits, because MUNIDESK-712 landed that call site.
- MEASURED 2026-07-31 (planner, timed a replica journal) — the cursor boundary check aggregates every thread to validate one. On a replica holding 400 threads, 400 runs, and 160,000 events, the boundary check at `src-tauri/core/src/journal/thread_summaries.rs:132` costs 3.5ms and the page query beside it costs 5.9ms. On a replica holding 4,000 threads, 8,000 runs, and 400,000 events, the same pair costs 39.1ms and 62.6ms. Reading that one thread's own maximum event time, its tombstone, and its workspace scope through the existing indexes returns the identical answer in 0.5ms. MUNIDESK-712 made this path production traffic twice over: the `Older threads` control pages with a cursor, and `refreshThreads` (`src/lib/chat-controller.js:66`) re-reads every loaded page after every settled run.
- SELECTED 2026-07-31 (this wave) — that rewrite. It changes no schema, no index, no cursor format, and no page shape.
- DONE 2026-07-31 — that rewrite landed (MUNIDESK-721). The cursor boundary check at `src-tauri/core/src/journal/thread_summaries.rs:132` reads the named thread's own maximum thread-event time, its runs' highest-`run_seq` event times, its tombstone, and its workspace scope. It no longer aggregates every thread to validate one cursor. Planner read 2026-07-31 confirmed the untouched page query beside it and the unchanged cursor format.

### Capability vocabulary and provenance
- DONE — user surfaces say “capabilities” and receipts render only server-supplied route/model/cost/time/capability provenance.

## Phase 3 — Routing metadata + voice (§9 items 12, 15)
- SUPERSEDED 2026-07-29 — cloud ingress classification replaces the completed local classifier contract.
- Voice direction is Parakeet verbatim capture with Kokoro read-aloud and global hotkeys.
- DONE — pinned Parakeet/Silero acquisition and publication, sherpa-onnx packaging/bindings, fixed-capacity microphone capture, bounded utterance segmentation, safe VAD boundary, chunk-invariant dictation composition, desktop command/event wiring from native capture through recognition with redacted statuses, basic composer dictation controls/transcript insertion, a reproducible target-hardware evaluator, a bounded 100-utterance endurance mode, and composer press-and-hold dictation with Escape-to-cancel/restore.
- DONE 2026-07-19 — accepted ADR 0014 pins the `ggml-org/llama.cpp` release `b10068` CPU-baseline archives for all four native targets, with a complete-entry-manifest extraction contract and pre-spawn re-verification.
- DONE 2026-07-20 — ADR 0014 implementation through activation: pinned llama-server descriptors for all four targets, manifest-verified safe extraction and reusable tree verification, bounded download with staging/lock/pointer publication, pre-spawn re-verification at the spawn boundary, supervised llama-server activation with health readiness, and third-party notice content; the required Qwen3.5-4B descriptor rides the same verified path with background required-download acquisition deliberately detached from onboarding.
- DONE 2026-07-22 — the desktop dictation-polish command, composer integration, and four post-dictation transforms against the activated resident server.
- DONE 2026-07-22 — fixed system-wide hold-to-talk shortcut foundation with press/release capture, safe busy-state handling, redacted registration failure, and teardown cleanup.
- DONE 2026-07-22 — rapid double activation promotes the composer button or global shortcut to hands-free dictation until Escape or the next activation.
- DONE 2026-07-22 — users can rebind the global voice shortcut with validated modifier-plus-key capture, collision-safe rollback, local persistence, and accessible shortcut exposure.
- DONE 2026-07-22 — accepted ADR 0015 pins the CPU-only Kokoro runtime, immutable model/voice artifacts, deterministic English text-processing and segmentation boundary, packaging contract, cancellation/privacy rules, and target-hardware validation gates.
- DONE 2026-07-22 — the pinned Kokoro model and voice bundle have a bounded resumable acquisition path and conflict-safe publication/current-revision resolution with full re-verification.
- DONE 2026-07-22 — normalized Kokoro source text packs into contiguous, bounded initial synthesis ranges against an injected exact phoneme/token-count boundary, including protected-run and Unicode-safe hard-split handling, followed by ADR 0015's one-pass sub-20-token repair against the same boundary.
- DONE 2026-07-22 — final repaired source ranges carry exact phoneme strings and token IDs through the same injected accent-aware boundary.
- DONE 2026-07-25 — `pack_initial_ranges` no longer re-evaluates the injected phonemize boundary for every same-rank candidate, so boundary calls are bounded rather than quadratic in the number of natural boundaries.
- DONE 2026-07-29 — ADR 0004 now pins muniment's own Parakeet conversion (MUNIDESK-678), so the desktop stops depending on a third-party conversion for its ASR graphs. The pinned sherpa-onnx runtime and the conversion settings, tooling, and source all carry byte-level identities.
- No further ungated Kokoro slice is selected. Native runtime/G2P packaging remains subject to ADR 0015's redistribution approval gate; synthesis, playback, response controls, and physical target-hardware runs remain separately deferred.

## Companion execution surfaces (§13)
- DONE E0/E0.5 — governed protocol, pairing, authorization, idempotency, bounded transport, ADR 0011 repository/lane decision, and ADR 0012 per-user runtime-service direction.
- DONE E1 — Rust CLI lane; Linux discovery/pairing; thread list/open; run start with ordered progress and flow control; typed permission handling; tool details; and receipts.
- DONE E2 — canonical fixtures and drift checks; TypeScript package/CI; cross-platform discovery, framing, pairing, authorization, thread documents, typed run streaming, workspace/current-file context, receipts, and fail-closed permission interaction.
- MEASURED 2026-07-29 (planner, ran the built `muniment-cli` binary) — the CLI answers no help request. `muniment --help`, `muniment -h`, `muniment --version`, and a bare `muniment` all print `muniment: unsupported arguments` plus the usage line to stderr and exit 1 (`src-tauri/cli/src/main.rs:11` and `:34-41`). A first-time user has no way to ask the program what it does. The usage string is also one 215-character line that repeats `muniment [--workspace <directory>]` four times, so an 80-column terminal wraps it into a block. DONE 2026-07-29 — the help slice landed anyway (MUNIDESK-658), ahead of the ruling that defers the rest of the CLI. Planner check 2026-07-30 ran the built binary: `--help` prints a wrapped usage block and exits 0, `--version` prints `0.0.1` and exits 0, and a bare invocation prints the error plus the block and exits 1.
- RETIRED 2026-07-29 — the completed E2 first-party editor extension will not be published or developed further. ADR 0022 will decide the disposition of the existing `editor-extension/` directory. The runtime service remains the sole runtime/session/journal owner.

### ACP interop, and the retired first-party editor extension (owner ruling 2026-07-29)
- OWNER RULING 2026-07-29 — muniment takes the ACP agent direction. muniment exposes the Agent Client Protocol, so any ACP editor drives the muniment runtime. The completed E2 first-party editor extension is RETIRED. The muniment CLI surface is DEFERRED indefinitely under the same ruling. An ADR records the direction before any ACP code.
- MEASURED 2026-07-29 (planner, web research against the protocol site and the package registries) — the owner's facts hold, and three of them change the shape of the decision. ACP is Apache-2.0 and speaks newline-delimited JSON-RPC 2.0 over stdio. The CLIENT spawns the AGENT as a subprocess, so an ACP editor starts a muniment process rather than connecting to one. Protocol version 1 is stable and negotiates as one integer in `initialize`. The v2 schema draft published 2026-07-20 states that its wire protocol may change incompatibly in any SDK release. The Rust crate `agent-client-protocol` published 2.0.0 on 2026-07-23 with MSRV 1.88.0, and `@agentclientprotocol/sdk` published 1.3.0 on 2026-07-21 with zero runtime dependencies. Both SDKs now live outside the protocol monorepo, in `agentclientprotocol/rust-sdk` and `agentclientprotocol/typescript-sdk`, so a dependency pin names the SDK repository rather than the spec repository.
- CONSTRAINT 2026-07-29 (planner) — ACP inverts two of this repository's ownership rules, so the ADR must answer both. First, the editor spawns the agent, while ADR 0012 makes the per-user runtime service the single owner of runtime state, so the spawned process cannot become a second executor. Second, ACP hands filesystem and terminal capability to the CLIENT: the agent calls `fs/read_text_file`, `fs/write_text_file`, and `terminal/*` back into the editor, and it asks the editor for permission through `session/request_permission` with the four option kinds `allow_once`, `allow_always`, `reject_once`, and `reject_always`. muniment owns filesystem access, tool execution, and the authoritative journal, and `chat_answer_permission` plus the `permission.requested` and `permission.resolved` events are how a native gate resolves. The ADR must state whether a gate answered inside a third-party editor satisfies a muniment grant check, and what the receipt records when it does. The protocol documents cancellation for a pending permission request and documents no timeout.
- SEQUENCED 2026-07-29 — the document splits in two, the way ADR 0020 did. Ownership, session identity, governance, the wire subset, capability negotiation, the version stance, and the dependency pins are more decisions than one reviewable ticket carries. Slice 1 decides who speaks ACP and how an ACP session stays governed, including the composition with ADR 0009, ADR 0011, and ADR 0012, and the disposition of the `editor-extension/` directory. Slice 2 decides the supported v1 method subset, the capability answers for `fs` and `terminal`, the v1-versus-v2-draft stance, the pinned SDK versions, and which editors the release claims. Slice 1 carries the governance statement, because governance is what lets an ACP session exist at all. ADR 0021 is reserved for the open on-device classifier store ADR, so the ACP document takes 0022.
- DONE 2026-07-29 — slice 1 landed as ADR 0022 (MUNIDESK-673). The Muniment ACP adapter is the only component that speaks ACP, and it is a thin `muniment.attach/1` client of the ADR 0012 runtime service. That service keeps sole ownership of sessions, threads, runs, effects, and receipts, so the editor-spawned process never becomes a second executor. An editor answer to `session/request_permission` is an input to a Muniment gate rather than a grant. The adapter enters `THREAT_MODEL.md` as a new local client identity whose claimed name, session ID, and `cwd` carry no authority. Planner review 2026-07-30 confirmed the composition with ADRs 0009, 0011, and 0012, the reserved `editor-extension/` disposition, and the explicit statement that the slice selects no SDK and pins no version.
- DONE 2026-07-30 — slice 2 landed (MUNIDESK-683). ADR 0022 now names the client-to-agent subset (`initialize`, `session/new`, `session/load`, `session/prompt`, the `session/cancel` notification) and the agent-to-client subset (`session/request_permission`, the `session/update` notification), and it lists every excluded v1 method with its reason. The filesystem and terminal client-capability answers are both no, the adapter negotiates the integer `1`, and Muniment does not adopt the v2 draft. The pin is the Rust crate `agent-client-protocol` at exactly `2.0.0` under ADR 0019, and the first release claims Zed and JetBrains IDEs after release validation. Planner check 2026-07-30 against the crates.io index confirmed the pin is real: `agent-client-protocol` 2.0.0 published 2026-07-23, declares `rust_version` 1.88.0, keeps every `unstable_*` feature (including `unstable_protocol_v2`) out of `default`, and pulls `agent-client-protocol-schema` 1.5.0 plus an async-io/async-process/futures/schemars tree.
- SELECTED 2026-07-30 (this wave) — the first ACP implementation slice. It creates the adapter crate, pins the SDK in the manifest and the lockfile, and answers `initialize` alone. Every session method fails closed, because ADR 0012's runtime service is still a decided direction rather than a built surface, and ADR 0022 already says an ACP request without an authorized service attach fails closed. The crate follows ADR 0011's companion boundary: it depends on `muniment-attach` rather than on `muniment-core`, the desktop crate, or Tauri. Pinning the SDK first is what makes the later attach slice a protocol translation instead of a dependency experiment.
- DONE 2026-07-30 — that slice landed (MUNIDESK-686). `src-tauri/acp/` holds the `muniment-acp` binary crate, the workspace lists it, and `agent-client-protocol` is pinned at `=2.0.0` in the manifest and the lockfile. The adapter answers `initialize` with protocol version 1 and `loadSession: true` alone, and every session method returns the fail-closed error `no authorized Muniment runtime attach exists`. Planner check 2026-07-30 confirmed the four stdio tests, the advertised capability set, and the CI wiring that runs `cargo fmt`, `cargo clippy`, and `cargo test` over the new package.
- MEASURED 2026-07-30 (planner, read the adapter against the attach client) — the adapter depends on no Muniment crate yet, so ADR 0011's companion boundary is a plan rather than a fact. `handshake_as_with_credential` (`src-tauri/attach/src/client.rs:1024`) hard-codes `kind: "cli"` in its Hello. That claimed kind travels into the connection binding (`src-tauri/core/src/attach/linux.rs:904`) and then into run provenance as `companion_kind` (`src-tauri/src/attach_service.rs:393`). An adapter that paired today would name itself the CLI in every receipt, against ADR 0022's rule that the runtime names the ACP adapter in approval and receipts.
- SELECTED 2026-07-30 (this wave) — the second implementation slice. The attach client takes its client kind from the caller, the adapter claims `acp-adapter`, and `session/new` pairs over `muniment.attach/1` and returns an ACP session ID bound to the request `cwd`. `session/load` and `session/prompt` stay fail-closed, because prompt translation and `session/update` streaming are their own slice.
- DONE 2026-07-30 — that slice landed (MUNIDESK-690). `handshake_as` and `handshake_as_with_credential` take the claimed client kind from the caller, `muniment-cli` passes `cli`, and the adapter passes `acp-adapter`. `session/new` rejects a relative `cwd` and any MCP server, pairs over `muniment.attach/1`, onboards the request `cwd` as the workspace, and returns a UUIDv7 session ID. Planner check 2026-07-30 confirmed the Hello-kind assertion in the Linux stdio test and the redaction assertions that keep the runtime path and the rejected server command out of both error messages.
- MEASURED 2026-07-30 (planner, read the adapter against the CLI pairing path) — the adapter asks the user to approve it again on every launch. `new_session` (`src-tauri/acp/src/main.rs:79`) calls `handshake`, which mints a fresh identity through `fresh_request_id()` and presents no credential, so `authorize_client` (`src-tauri/src/attach_service.rs:300`) meets an unknown client and the desktop raises a new approval prompt. The CLI answered this already: `authorized_client_identity` and `authorized_client_credential` (`src-tauri/cli/src/main.rs:174-205`) keep a stable identity and its issued credential under `$XDG_CONFIG_HOME/muniment/`, and `persist_authorized_client_credential` rewrites the credential after every handshake, so a repeat run reconnects with no prompt. An ACP client spawns one agent process per session, so the adapter meets that prompt far more often than the CLI does. SELECTED 2026-07-30 (this wave) — the adapter's own persisted identity and credential, in the CLI's shape.
- DONE 2026-07-30 — that identity landed (MUNIDESK-695). The adapter keeps `acp-client-id` and `acp-client-credential` under `$XDG_CONFIG_HOME/muniment/`, presents the credential on every handshake, and rewrites it after each one, so a repeat launch reconnects with no approval prompt. Planner read 2026-07-30 of `src-tauri/acp/src/main.rs` confirmed the 0600 private-file check, the `O_NOFOLLOW` read, and the atomic credential write.
- MEASURED 2026-07-30 (planner, read the attach run stream against the ACP session-update contract) — `session/prompt` cannot stream assistant text over `muniment.attach/1` today. `append_run_stream_page` (`src-tauri/core/src/attach/linux.rs:1434`) sends every non-permission event with the payload `{"withheld": true}` plus an optional receipt, and `RedactedRunEvent` (`src-tauri/attach/src/client.rs:107`) carries `run_seq`, `event_type`, `event_version`, `recorded_at`, and `receipt` alone. ACP's `session/update` needs `agent_message_chunk` text. `thread.open` does carry text, because `RedactedThreadEntry` has a `text` field, so the reply is reachable after the run rather than during it. The prompt slice therefore waits on a design call with two candidates: the attach protocol grows an authorized text-bearing run event, or the adapter reads the thread page after the terminal event and emits one `agent_message_chunk`. Not selected this wave, because that is a protocol decision under ADR 0009 rather than a translation slice.
- SELECTED 2026-07-30 (this wave) — that decision, as a dated amendment inside ADR 0009. The amendment picks one candidate, states the projection and its bounds, states whether the run stream and `thread.open` disclose the same content, states the resubscribe rule that keeps one `agent_message_chunk` per text projection, and names the first implementation slice. It writes no code, because the protocol answer gates both the attach change and the adapter translation.
- DONE 2026-07-30 — that amendment landed (MUNIDESK-698). Each committed `model.stream.delta` projects as a text-bearing `run.event`. `payload.text` holds at most 65,536 UTF-8 bytes. One stateful projector is the single redaction source for the run stream and for `thread.open`. A resubscribe rebuilds projector state through the companion's cursor and re-emits nothing. Planner read 2026-07-30 confirmed the rejected `thread.open` alternative, the withheld-delta rule, and the named first slice.
- SEQUENCED 2026-07-30 (planner) — that named first slice holds six pieces. It names the journal split, the canonical text projector, `payload.text` on the attach run event, the shared `thread.open` projection, the byte-window accounting, and the ACP `agent_message_chunk` translation. Six pieces across the journal, the protocol, and the adapter is more than one reviewable ticket carries. The work therefore splits, and slice 1 is the journal split alone. That slice needs no redaction rule and changes no protocol field.
- MEASURED 2026-07-30 (planner code read) — the desktop bounds no committed delta today. `chat_coordinate.rs:486` hands each Pi delta straight to `append_emit` (`src-tauri/src/chat_coordinate.rs:790`), and that call is the only production writer of the event. The second `model.stream.delta` writer, at `src-tauri/src/attach_service.rs:786`, is test fixture code. The repository also holds no secret, path, or content redaction rule for assistant text. The projector slice must carry that rule set, so it follows the split rather than leading it.
- DONE 2026-07-31 — that slice landed (MUNIDESK-703). A committed `model.stream.delta` carries at most 65,536 UTF-8 bytes, `append_emit` splits a larger delta at scalar boundaries into ordered envelopes, and it writes no empty envelope. Planner check 2026-07-31 confirmed the split at `src-tauri/src/chat_coordinate.rs` and the reducer tests that cover a multi-byte scalar on the boundary.
- MEASURED 2026-07-31 (planner, grep over `src-tauri/`) — the redaction rule that the projector applies does not exist. Two functions carry `redact` in their names, `redact_parakeet_failure` (`src-tauri/src/model_install.rs:234`) and `redact_capture` (`src-tauri/src/dictation.rs:312`), and neither one reads assistant text. `thread.open` already discloses assistant text unredacted, because `RedactedThreadEntry.text` (`src-tauri/core/src/attach/linux.rs:416`) passes the stored projection straight through. The repository's one declared secret-pattern set is `.gitleaks.toml`, and it guards committed source rather than runtime text. The amendment therefore names a rule that no code and no document defines.
- SELECTED 2026-07-31 (this wave) — that rule set, as a second dated amendment inside ADR 0009. It names each rule, its finite maximum match span, and the retained-suffix bound those spans produce. It also states what happens to assistant text already committed before the rule lands, because `thread.open` discloses that text today. The projector slice follows it, and the projector stays unfileable until it does.
- DONE 2026-07-31 — that amendment landed (MUNIDESK-707). ADR 0009 carries the `assistant-text-v1` rule set: four `secret.*` rules, two `path.*` rules, and the `content.withheld` metadata rule, each with a finite maximum span. The greatest span is 131,068 bytes, so the projector retains at most 131,067 UTF-8 bytes. The canonical projector reprojects every committed assistant delta on a `thread.open` read rather than rewriting the journal. Planner read 2026-07-31 confirmed the closed provider alternation and its derivation from the gitleaks 8.30.1 pin in `.github/workflows/secret-scan.yml`.
- SEQUENCED 2026-07-31 (planner) — that amendment's first slice still holds four pieces. It names the committed disclosure fields, the pure matcher, the stateful retained-suffix projector, and the golden fixtures, before the attach run stream and `thread.open` read the projector. Four pieces across the journal, a new matcher, and the protocol is more than one reviewable ticket carries, so the work splits again.
- SELECTED 2026-07-31 (this wave) — the pure matcher for the four `secret.*` rules alone. The two `path.*` rules need the companion's approved workspace and a canonical scope check, and `content.withheld` needs journal fields that no writer commits yet, so each takes its own slice. `muniment-core` carries no regex crate and hand-writes every byte grammar it already matches, so this slice adds no dependency.
- RE-DISSECTED 2026-07-31 (planner) — the implementer handed that matcher back after repeated attempts, so it splits again. One ticket asked for four byte grammars, cross-rule arbitration, a streaming retention offset, and over-span handling. `secret.assignment` alone carries twelve case-insensitive labels, an identifier boundary on each side, a 20-byte filler run, nine delimiters, a five-byte quote run, a 150-byte value, and six terminators. The retention offset is the harder half. It is a property of every rule at once, so it cannot be written correctly until every rule exists, and every rule cannot land in one reviewable diff. The split inverts that order. Slice 1a settles the scan contract and proves it on `secret.provider-token`, whose eight alternatives are pure prefix, alphabet, and length checks. `secret.pem-private-key`, `secret.jwt`, and `secret.assignment` then each add one rule to a settled contract, and cross-rule arbitration lands with the second rule.
- SELECTED 2026-07-31 (this wave) — slice 1a alone.
- DONE 2026-07-31 — slice 1a landed (MUNIDESK-714). `src-tauri/core/src/assistant_text.rs` holds the scan contract, and `scan` returns ordered non-overlapping matches, a streaming `retention_offset`, and a `withhold_from` offset for an over-span candidate. Every `secret.provider-token` alternative matches through prefix, alphabet, length, and boundary checks alone, and `muniment-core` gained no dependency. Planner read 2026-07-31 confirmed the seven tests in `src-tauri/core/tests/assistant_text_secrets.rs`, including the incomplete candidate that waits for its terminator.
- DONE 2026-07-31 — the committed disclosure fields landed (MUNIDESK-724). `chat_coordinate.rs` writes every `model.stream.delta` with `content_disclosure` set to `released`, and `src-tauri/core/src/journal/content_disclosure.rs` reads that field back. A missing, unknown, or contradictory value reads as withheld. Planner read 2026-07-31 confirmed the split-delta test that asserts the field on each envelope, and confirmed that no projector consumes the reader yet.
- SELECTED 2026-07-31 (this wave) — slice 1b, the `secret.pem-private-key` rule and the cross-rule arbitration it makes observable. That rule is one bounded BEGIN-body-END grammar over six header names, so it adds no alternation table. Arbitration lands with it, because a second rule is what makes longest-match-then-rule-order testable. The slice also generalizes the retention offset to the greatest implemented span, which rises from 512 bytes to 65,536.
- DONE 2026-07-31 — slice 1b-1 landed (MUNIDESK-723). `scan` now dispatches every entry of `RULE_ORDER` at each byte. `arbitrate` picks the longest candidate and then the earliest rule. `retention_offset` derives from the greatest implemented `max_span` rather than from one rule's constant. Planner read 2026-07-31 of `src-tauri/core/src/assistant_text.rs` confirmed the dispatch loop at line 81 and the arbitration test. `RULE_ORDER` still holds `Rule::SecretProviderToken` alone, so the pem rule itself did not land.
- SELECTED 2026-07-31 (this wave) — slice 1b-2, the `secret.pem-private-key` rule alone. It is one bounded BEGIN-body-END grammar over six header names, so it adds no alternation table and no dependency. It makes the landed arbitration observable against real input. It also raises the retained suffix from 512 bytes to 65,536, through span accounting that already reads every rule.
- RE-DISSECTED 2026-07-31 (planner) — the implementer handed slice 1b-2 back after repeated attempts, so it splits again. Three traps sat inside one ticket. First, its arbitration criterion asked for a test where a provider token and a pem candidate start at the same byte, and no such input exists. Every provider prefix opens with an alphanumeric byte, and the pem rule opens with `-`. Second, raising `max_span` to 65,536 breaks a landed test that the ticket never named: `reports_complete_and_incomplete_retention` (`src-tauri/core/tests/assistant_text_secrets.rs:78`) pins the retained suffix at 512 bytes. Third, over-span classification is a separate concern from grammar recognition, because it answers what happens to a `BEGIN` line that never terminates.
- SELECTED 2026-07-31 (this wave) — slice 1b-2a, the grammar alone. It adds the rule, its six header names, the body class, the same-name `END` line, and the raised retention bound. Content that ends before the grammar completes returns no match. Slice 1b-2b follows and classifies an unterminated `BEGIN` line as an over-span candidate that sets `withhold_from`. The 65,460-byte body cap plus the fixed line lengths already hold a complete match under 65,536 bytes, so slice 1b-2a needs no span counter. The observable cross-rule case is a pem body holding an `AKIA` credential, because the base64 body alphabet admits one and the pem match consumes it.
- NOTED 2026-07-31 (planner, read the merged matcher) — `alternatives_at` (`src-tauri/core/src/assistant_text.rs:191`) chains three provider alternatives onto the 15-entry `ALTERNATIVES` table. All 18 belong in one place. The behavior is correct and the split carries no stated reason. The next slice that edits that table folds them in rather than taking a ticket of its own.
- MEASURED 2026-07-30 (planner, ran the workspace suites) — the pinned ACP SDK changes JSON key order for every crate built beside it. `agent-client-protocol` 2.0.0 pulls `schemars` 1.2.1, which enables `serde_json/preserve_order`, and Cargo unifies that feature across one invocation with no per-crate opt-out (serde-rs/json issue 1162). `cargo test -p muniment-core` passes alone. `cargo test --no-fail-fast -p muniment-core -p muniment-acp -p muniment-attach -p muniment-cli` reports 659 passed and 1 failed: `transport_send_completes_while_call_waits` (`src-tauri/core/tests/pi_chat.rs:705`) reads back `{"type":…,"id":…,"confirmed":true}` where it expects the sorted `{"confirmed":true,"id":…,"type":…}`. Today's CI never builds core beside the adapter, so both lanes stay green. The real exposure is the native sign-in transcript: `native_authorization.rs:353` hashes `serde_json::to_vec(&unsigned)` over a `serde_json::Map`, so sorted key order is a cargo-feature accident rather than a written rule, and `native_token.rs:182` recomputes the transcript exactly as the code does, so no test would catch the change. SELECTED 2026-07-30 (this wave) — pinning that order in code and guarding it in CI.
- DONE 2026-07-30 — that pin landed (MUNIDESK-694). `begin_native_authorization` collects the unsigned sign-in body into a `BTreeMap`, so sorted key order is a written rule rather than a cargo-feature accident, and its test asserts the exact canonical bytes. CI gained a `Core and ACP unit tests` step that builds `muniment-core` beside `muniment-acp` in one cargo invocation. Planner check 2026-07-30 ran `cargo test --no-fail-fast -p muniment-core -p muniment-attach -p muniment-cli -p muniment-acp` from a clean clone: 660 tests across 55 binaries passed with no failure.
- MEASURED 2026-07-31 (planner, ran the same invocation as a check) — a second build-composition accident sits beside that one. This one decides a type rather than a byte order. `src-tauri/core/src/attach/mod.rs:14` glob re-exports `muniment_attach::*` beside `pub use cursor::*` at line 12. `muniment-attach` gates its client module behind the `client` feature, and that module defines `RunStreamWindow` (`src-tauri/attach/src/client.rs:91`). `src-tauri/core/src/attach/cursor.rs:10` defines a different struct under the same name. The cursor struct derives `Copy` and no `Deserialize`. The client struct derives `Deserialize` and no `Copy`. `muniment-core` does not enable the feature, and the desktop crate does (`src-tauri/Cargo.toml:44`), so Cargo's per-graph feature unification decides which struct `muniment_core::attach::RunStreamWindow` names. `cargo check -p muniment-core --all-targets` reports nothing. The same check over the four packages together reports `warning: ambiguous glob re-exports` and names the attach client struct the winner. No lane fails on it. The clippy step at `.github/workflows/ci.yml:167` builds core alone. The core-beside-adapter step at `:175` runs `cargo test`, which passes a warning through.
- SELECTED 2026-07-31 (this wave) — removing that collision and adding the clippy step that would have caught it.
- NOTED 2026-07-29 (planner) — the CLI help-output slice is deferred indefinitely. E1's landed CLI code and `editor-extension/` both stay in the tree until the accepted ADR names their disposition.

## Cross-surface contracts (owner ideas 2026-07-29)
- OWNER IDEA 2026-07-29 — two decision documents open this lane. Both start as ADRs, and both cross the desktop, the CLI, the editor extension, and the mobile companion.
- DONE 2026-07-29 — the E0 contract source-of-truth ADR landed as ADR 0019 (MUNIDESK-626). Every surface used to vendor its own copy of each published cloud contract, and the hand copies drifted. MUNIMOBILE-240 and MUNIMOBILE-227 each vendored a contract by hand, and MUNIMOBILE-610 closed the chain by vendoring the shipped fixture. ADR 0019 puts the canonical schemas in `mikeydiamonds/muniment-cloud` under `contracts/e0/<contract>/<major>/`. TypeSpec is the source format, and the lane emits OpenAPI 3.0. One generator run per language produces a versioned Rust crate and a versioned TypeScript package. Generation runs only in the cloud publication lane, and every surface pins one exact artifact version. Planner review 2026-07-29 confirmed the composition with ADR 0009 and ADR 0011 and the migration path for the contracts already vendored.
- SEQUENCED 2026-07-29 — the code diff renderer ADR follows it, and it splits in two. A user cannot approve an edit that the surface cannot show. No DOM library draws in a terminal, so the expected shape is one diff model with a renderer per surface. The owner idea asks one document to decide three surfaces, measure five candidates against nine web bars plus four terminal bars, and name its composition with the contract ADR. Thirteen measured bars across two rendering technologies is more than one reviewable ticket carries. Slice 1 therefore decides the shared diff model and the two web renderers, including whether E2 uses the native VS Code diff editor. Slice 2 decides the terminal renderer for E1 and records whether each web candidate runs under React Native. Slice 1 waited on the contract ADR, because the diff model is itself a cross-surface contract.
- CORRECTED 2026-07-29 (planner) — the diff-view spec citation above named the wrong section. harness-spec §6.5 covers filesystem access and sandboxing. §6.1 is the line that renders tool activity, including diffs, inline on the one conversation surface. §13.1 then makes the CLI and the editor extension additional windows onto that same surface, so each one must show the same diff.
- DONE 2026-07-29 — that slice landed as ADR 0020 (MUNIDESK-630). The shared contract is `code-diff/1`, and its canonical TypeSpec schema lives in the cloud repository under ADR 0019's rules. The desktop renders through the `diff2html` parser and HTML generator with a design-token override sheet, and it adds no React. The editor extension draws no second web diff: it exposes each side as a read-only virtual document and runs the built-in `vscode.diff` command. Planner review 2026-07-29 confirmed the nine-bar candidate table, the size-limit and truncation rules, and the untouched terminal decision.
- DONE 2026-07-29 — that slice landed (MUNIDESK-640). ADR 0020 now carries a six-candidate terminal table, and the CLI selects a hand-written standard-library renderer. That renderer reads `CodeDiff` directly, so it needs no diff algorithm, and it adds no crate. `test/cli-dependency-boundary.sh` and its allowlist stay unchanged. The renderer writes ANSI color only when stdout is a terminal and `NO_COLOR` is absent, never wraps or truncates a line, and names explicit output for binary, empty, and truncated diffs. The ADR also records that no evaluated web renderer runs unchanged under React Native. Planner review 2026-07-29 confirmed the terminal table, the untouched-allowlist claim, and the slice list.

### The on-device classifier store (owner idea 2026-07-29)
- SUPERSEDED 2026-07-29 — the cloud owns prompt classification at ingress.
  Desktop clients send prompts without classification metadata and run no
  resident classifier.
- DONE 2026-07-30 — the desktop removed the Qwen descriptor, acquisition,
  startup worker, user interface, resident inference, and llama.cpp packaging.
  Parakeet remains unchanged.
- OWNER IDEA 2026-07-29 — a third decision document opens this lane, and it supersedes the cancelled MUNIDESK-643. The owner states the architecture. No surface executes a prompt locally. Every surface classifies the prompt on the device, then sends the prompt together with that classification to the muniment cloud, which routes to the customer's own model providers. Local classification is a pre-flight step, so it is not execution. Any surface that can send a prompt needs the classifier, and mobile can send prompts. Two stores follow. The desktop, the CLI, and the editor extension share one copy owned by the device core service. The mobile app carries its own copy.
- MEASURED 2026-07-29 (planner, repo read) — the ADR builds on machinery that already exists. `app.path().app_data_dir()` joined with `models/<name>/` is today's store root (`src-tauri/src/main.rs:32`). `ResidentModelDescriptor` pins `byte_size` and `sha256`, and `verify_model_artifact` refuses a mismatch (`src-tauri/core/src/llama.rs:90-118`). `NativeInstallLock` takes `install.lock`, staging writes a resumable `.part` file over HTTP `Range`, and `replace_revision` publishes atomically behind the `current`, `previous`, and `rejected` pointers (`src-tauri/core/src/llama/lifecycle.rs`). `checked_initial_url` and `checked_redirect_url` are the split host policy the MUNIDESK-571 incident produced (`src-tauri/core/src/model_acquisition_transport.rs:358-393`). The gap is ownership and cross-surface discovery, not download machinery.
- CONSTRAINT 2026-07-29 (planner) — `test/cli-dependency-boundary.sh` allows `muniment-cli` only `muniment-attach`, serde, uuid, and their support crates, and ADR 0011 forbids a `muniment-core` dependency. The CLI therefore cannot link an inference runtime. Slice 1 must say how the CLI reaches a classification without one. ADR 0012 already names the per-user runtime service as the single owner of runtime state, so the ADR composes with it rather than inventing a second owner.
- NOTED 2026-07-29 (planner) — three published rules stand against a mobile classifier today. harness-spec §12.1 says phones run no local models. §12.2 item 2 says mobile has no resident local classifier, so the routing label is produced server-side. ADR 0007 repeats the prohibition as the reason mobile lives in its own repository. The ADR must name all three and state why a pre-flight classification is a different thing. Amending those documents is owner work that follows the accepted ADR.
- SEQUENCED 2026-07-29 (planner) — the document splits in two, the way ADR 0020 did. Nine decisions across two platforms, two repositories, and two delivery mechanisms is more than one reviewable ticket carries. Slice 1 decides the shared desktop store: location and owner, second-surface discovery, pinning and verification, concurrent cold-start fetch, upgrade and eviction, offline and air-gapped installs, and the fetch host and redirect policy. Slice 2 decides the mobile store: its per-platform location, whether the artifact arrives as a post-install download or an install-time asset pack, and what mobile does before its model arrives. Slice 1 carries the section 12 statement, because that statement is what lets a mobile store exist at all.
- CLOSED 2026-07-30 — ADR 0021 landed as a superseded record rather than a store design, because the cloud ingress ruling arrived first. The desktop downloads and runs no classifier, so slice 1 has no subject. Slice 2, the mobile store, is void under the same ruling, and harness-spec §§12.1 and 12.2 and ADR 0007 stand unamended.

## Browser-control runtime capability (§6.8)
- OWNER GO 2026-07-16 — v1 browser-only actuator line is governed runtime capability, not a companion surface.
- DONE E0–E14 — extension-only real-profile architecture through injected memory-only pairing and MV3 authorized relay.
- BLOCKED E15 — desktop-to-extension handoff waits for the owner to upload a real package to the private Chrome Web Store draft and provide its public key. Do not generate a replacement key/ID. Reserved extension ID: `cdedcfbgomnhfpifpgdlpfkkanaofkjd`.
- After the matching key lands, compose and validate E15 as one slice; runtime tools, policy, receipts, and kill switch follow. Store publication/upload are owner-gated; v1 excludes OS, filesystem, and other-app control.

## Remote Control (§14) — a design study, not a build lane
- OWNER IDEA 2026-07-29 — study t3code's remote-control experience as a reference for interaction polish, then write a short design note plus polish tickets against the desktop remote-control surface. Inspiration only. No copied code, no Electron, and owner mockups stay design ground truth.
- CORRECTED 2026-07-29 (planner measurement) — this repository holds no remote-control code, so the polish half of that idea has nothing to attach to. A grep over `src/`, `src-tauri/`, and `test/` for `remote_control`, `remoteControl`, and `remote-control` returns zero matches. harness-spec §14 is the ratified specification, and §14.1 gives the single outbound relay leg to the ADR 0012 runtime service, which is a decided direction rather than a built surface. §12.2 item 3 records that journal-backed relay publication is not implemented, and ADR 0002 defers relay publication to its slice 5. The shipped MV3 relay is the browser-control loopback relay (§6.8), which is a different surface, and its E15 desktop handoff stays blocked on an owner upload. No polish ticket can be filed against a surface with no code.
- MEASURED 2026-07-29 (planner, web research) — the reference material is real and it converges. t3code documents a one-time owner pairing token that a remote device exchanges for a session, a shareable pairing link plus QR code, an endpoint list with a persisted default, and a paired-environment list carrying online, checking, and error states. Its own architecture document states that reconnection logic, latency compensation, and optimistic rendering are undocumented, so the polish the idea admires is partly unpublished. Claude Code's remote control prints a session URL and a QR code, runs the agent on the local machine, reconnects automatically for about ten minutes before the session ends, and marks an online machine with a status dot. That ten-minute window matches §14.1's bounded reconnect window already. VS Code Live Share holds steady-state session status in a persistent low-chrome status-bar item rather than a banner. Google Cast's connected state shows the receiver name beside a Stop Casting control and uses no alarm chrome.
- DONE 2026-07-29 — that note landed at `docs/design-reference/remote-control-ux.md` (MUNIDESK-674), with a one-line link from `DESIGN.md`. Planner check 2026-07-30 confirmed the note records the reference material, states plainly that the surface is unbuilt, and marks every proposed state as pending owner mockup confirmation.

## Onboarding, memory, and the Muniment Home (owner rulings 2026-07-19)
- RATIFIED — visible human-editable Markdown Home under `memory/`, `agents/`, `projects/<name>/`, `sessions/`; files are source of truth, indexes are rebuildable caches, no hidden primary store or Muniment sync, semantic transcript names and visible retention.
- RATIFIED — desktop has an unskippable first-run directory picker (default `Documents/Muniment`); CLI/VS Code use the opened directory by default and lazily create user Home for cross-project need. Honor nearest `AGENTS.md`; instructions and memory remain distinct.
- SUPERSEDED 2026-07-29 — the required resident model no longer serves onboarding triage or routing. Import retains consent, preview, provenance, and verbatim originals.
- RATIFIED — agent system prompt has five binding rules, hand-written/versioned/eval-gated base v0, and a bounded audited labeled-data tail.
- HISTORICAL 2026-07-20 — the superseded required-model descriptor and background acquisition landed.
- DONE 2026-07-21 — first-run Home picker/scaffold; bounded ZIP preview and manifest review; bounded extraction of explicitly selected entries with verbatim text and stable provenance; consent checklist and Tauri extraction bridge retaining approved content only in transient pre-triage state. No Home writes or model calls occur in these slices.
- DONE 2026-07-21 — typed, bounded local-model triage request/report-validation contract over approved extracted entries, plus its typed Tauri invocation against the resident local model.
- DONE 2026-07-21 — structured local-triage report review, approved-source display, redacted retry handling, and explicit in-session confirmation; no Home writes occur in this slice.
- DONE 2026-07-22 — confirmed triage reports, starter-agent proposals, and provenance-bearing verbatim originals compile into a deterministic bounded Home write plan without filesystem access.
- DONE 2026-07-22 — onboarding reports required local-model progress/readiness without blocking Home setup and gates local proposal generation until AI features are available.
- DONE 2026-07-24 — a confirmed bounded Home write plan persists conflict-safely without overwriting user files, with rollback, concurrency serialization, and symlink defenses.
- DONE 2026-07-24 — compilation and persistence are exposed through one typed Tauri completion command with structured invalid-input, destination-conflict, and save-failure results.
- DONE 2026-07-24 — the confirmed-proposal frontend invokes the native completion command once, handles success/invalid input/save failure, and recovers destination conflicts by choosing another Home without repeating preview or triage.
- DONE 2026-07-25 — a failed post-publish temporary-file cleanup no longer promotes itself into the save result, so the onboarding-import files that were just written correctly survive.
- DONE 2026-07-24 — the artifact-rail control is a real signed-in frontend shell with button/platform shortcut toggle, Escape close, lifecycle reset, and an honest empty state.
- DONE 2026-07-24 — the open artifact rail is adjustable from 380–560px with an accessible pointer/keyboard splitter and viewport-aware bounds.
- DONE 2026-07-24 — the sidebar collapses with `⌘\` / `Ctrl \` in 180ms to a 52px tooltip icon rail and remembers its state per device.
- DONE 2026-07-25 — streamed replies announce one coarse string per run phase instead of re-reading the whole thread to screen readers on every chunk.
- DONE 2026-07-25 — the provenance line matches the owner mockup: `route → model · cost · time` with `--signal` bound to the route field rather than to whichever part is first, tabular numerals, and 11.5px mono.
- DONE 2026-07-25 — `docs/spec/02-desktop-app.md` §4's composer grows from two rows to a ten-line cap and then scrolls, holding the polish overlay and the thread's scroll position steady through every resize, including rail drags and sidebar collapse.
- DONE 2026-07-25 — §3.2's quiet per-message action row ships Copy alone, revealed on hover or focus, holding its place in the tab order and announcing through its own live region rather than the run-phase one. Fork, share, and retry still wait on the thread/branching contract.
- DONE 2026-07-25 — §2.3/§4's Send is the ink primary action in both composer states, and the composer takes focus once on entry to the signed-in workspace.
- DONE 2026-07-25 — §1.2's color law holds in code: one global 2px ink focus ring at offset 2, no accent on at-rest thread dots or the model-download fill, and `src/styles/signal-allowlist.test.js` is §7's promised lint over every component's styles and markup.
- DONE 2026-07-25 — §1.3/§2.1's per-device System / Light / Dark override lives in the profile popover, applied before mount and persisted per device.
- DONE 2026-07-25 — `src/styles/base.css` now carries the ground-truth bundle's element defaults (`::selection`, ink links, heading weight/leading/tracking, mono tabular numerals) and one blanket `prefers-reduced-motion` rule, so motion compliance no longer depends on a hand-maintained transition list.
- DONE 2026-07-25 — light `--signal` is the spec's `#2A7264` in both the OS-default and explicit-light blocks, with the token matrix guarded by `src/styles/contrast.test.js`.
- DONE 2026-07-25 — `syncComposerHeight` no longer uses focus as a proxy for "the user typed this", so dictated text written past the ten-line cap scrolls into view with the composer focused.
- AUDIT 2026-07-25 (code review over everything merged since the 2026-07-09 audit) — three confirmed defects filed, all three now landed: the Home-import cleanup-to-rollback promotion, the focused-composer dictation scroll, and the quadratic Kokoro packing boundary calls. Noted, not filed: `natural_candidates` (`src-tauri/core/src/kokoro/packing.rs:375-379`) suppresses the rank-3 word boundary after *any* closer, where ADR 0015 §segmentation only suppresses a closer "already covered by rank 1 or 2"; the ADR wording and the intended behaviour want an owner/author call before the one-line change.
- DONE 2026-07-26 — the signed-in workspace no longer paints the centered sign-in lockup or `shell v0.0.1` behind the conversation; `.signed-frame` is a real rule.
- DONE 2026-07-26 — the Appearance buttons take the global ink focus ring, and the §1.2/§6 guard now walks every component under `src/` rather than `base.css` alone, with `.artifact-divider` as the one documented exception.
- DONE 2026-07-26 — the onboarding surface lives in `src/lib/Onboarding.svelte`; `src/App.svelte` is down to ~1570 lines.
- DESIGN CALL 2026-07-26 (planner, §2.2 "active line") — the active line is the last line box of the streaming response's prose, and the 2px signal rule runs from that line's start to the caret, following the text as chunks arrive and leaving when the run leaves `streaming`. CSS cannot express it: measured against the built bundle at 1280×820, a three-line streaming reply reports three line rects (`x: 390`, `y: 539/563/586`) and Chromium paints `border-bottom: 2px solid rgb(42,114,100)` on every one of them — `box-decoration-break` governs the inline-direction edges only — so the rule has to be a measured overlay driven by a pure geometry helper. §2.2's 92% measure applies to the response prose; tool cards and the receipt stay at the 760px column width because they are records, not prose.
- DONE 2026-07-26 — §2.2's streaming rule is a measured overlay that marks the active line alone (`src/lib/streaming-underline.js`), and response prose takes the 92% measure while tool cards and the receipt keep the 760px record column.
- DONE 2026-07-26 — §1.5/§4 shape and depth are tokens: every component references the radius scale (`--radius-panel` included), the two improvised `color-mix` shadows are gone in favour of black-based `--shadow-window`/`--shadow-overlay` identical in both themes, and `src/styles/shape-scale.test.js` lints both scales the way `signal-allowlist.test.js` lints §1.2.
- DONE 2026-07-26 — the profile popover fits its window: `src/lib/AccessPanel.svelte` is a fixed header, one scrolling content region, and a fixed footer holding Sign out, in 02-desktop-app §2.1's order, with structural layout assertions in `src/lib/access-panel.test.js`. Planner render 2026-07-26 of the built bundle at the shipped 1100×720 default confirms the fix on screen: the popover bottoms out at 639px, Sign out renders at y≈603–629, and only `.access-content` scrolls.
- DONE 2026-07-26 — shell chrome keyboard hints: the sidebar chips derive their modifier from the same platform helpers the tooltips use, the titlebar artifact toggle carries a visible “Artifacts” label beside its chip, and every chip sits in the `kbd` mono-muted register. Confirmed in the same planner render.
- DONE 2026-07-26 — the undefined-class lint (`src/styles/class-usage.test.js`, the MUNIDESK-508 shape) walks every component's markup against its style rules, and the four pre-existing undefined classes (`voice`, `attach`, `import-card`, `revoked`) are cleared.
- DONE 2026-07-26 — design law §1.4: the 15 raw font sizes (9 `font-size` declarations and 6 `font:` shorthands) resolve through `--text-*` scale tokens, §2.2's 11.5px mono provenance register is a named off-scale token rather than a lint exception, and `src/styles/type-scale.test.js` permits component font sizes through `var(--text-*)` only, covering both declaration forms in the shape/signal lint idiom.
- DONE 2026-07-26 — §2.1 profile popover register: the fixed header is the identity (display name in grotesque, `org · role` mono muted — the sidebar block's register), the dialog and close labels follow it, `Snapshot vN` lives inside the Your-access section as the mono record it is, the doubled `Your access`/`Your groups` headings are collapsed, and `.profile-button small` truncates a UUID org fallback to one ellipsized line with the full string on `title`. Planner render 2026-07-26 of the built bundle (stubbed signed-in `window.__TAURI__`, 1100×720) confirms the header identity, section order, and single-line profile subline on screen.
- DONE 2026-07-26 — sidebar honesty: the dead `New thread` and `Search` controls and their `⌘N`/`⌘F` shortcut chips are absent until the thread/branching and palette/search contracts, respectively, land; `src/App.test.js` asserts their absence in both sidebar states.
- DONE 2026-07-26 — §1.8 thinking-chip reduction: the 17px thinking surface uses the parametrically generated solid two-edge milled mark, verdigris fill, a 180ms eased settle, and a static reduced-motion state. The full breath/spin/trace behavior engine remains parked until a ≥34px thinking surface exists; rest-state marks keep the owner SVG geometry untouched.
- DONE 2026-07-26 — code health: the global voice-shortcut manager's registration fallback, collision-safe rollback, persistence, and serialized changes live in the injectable `src/lib/voice-shortcut.js` factory with direct unit tests.
- VERIFIED 2026-07-26 (second planner render, stubbed signed-in bundle in headless Chromium) — the explicit Dark override themes every surface consistently (sidebar, popover, titlebar, and thread all measured at their dark token values, popover on `--paper` #141716), the §10 first-run line and composer focus, the signed-out lockup, the §2.1 popover order, and the revoked-device `--oxide` record all render per spec. An earlier read of a CLI screenshot suggesting a mixed light/dark render was wrong; the computed-style probe disproved it.
- VERIFIED 2026-07-26 (repeat render against the checked-out built bundle) — §3.1's user bubble keeps its intended asymmetry: `.thread` and `.user-turn` each measure 760px, while `.user-message` measures 592.797px with computed `max-width: 78%` (a 0.7800 ratio to the thread). The earlier observation compared the wrong computed style and is withdrawn; no bubble-width fix is selected.
- DONE 2026-07-26 — the artifact rail uses one accessible `Artifacts` h2; the redundant `Thread artifacts` eyebrow is gone, and `src/App.test.js` asserts the single heading and its `aria-labelledby` relationship.
- DONE 2026-07-26 — §1 platform chrome enforces the 960×640 window minimum in `src-tauri/tauri.conf.json`, and `tauri-plugin-window-state` restores window size and position across launches (with size-only restore where Wayland does not report outer position).
- DONE 2026-07-26 — the dictation session controller (capture epochs, status polling, quiet-period completion, polish, and transform expiry) lives in the injectable `src/lib/dictation-controller.js`; direct tests cover stale capture epochs, quiet-period completion, transform expiry/invalidation, and start/stop failures.
- DONE 2026-07-26 — §12’s two shipped chrome chords work from the workspace’s own entry state: the `!isEditableTarget` guard is gone from `isArtifactRailShortcut`/`isSidebarShortcut`, and the fix’s tests dispatch from the focused composer (MUNIDESK-531). VERIFIED same day by planner probe of the built bundle: with the composer holding entry focus, `Ctrl J` opened the rail and `Ctrl \` collapsed the sidebar to a measured 52px.
- DONE 2026-07-26 — profile-popover device rows render real platform names through `platformDisplayName` where `devicesReadyState` shapes the list, and the `text-transform: capitalize` register is gone (MUNIDESK-532). VERIFIED in the same probe: the revoked row reads “macOS”, the others “Linux”/“Windows”, active-first order intact.
- DONE 2026-07-26 — the chat run lifecycle (send/steer/cancel/resume/queue, history restore, buffered chat-event reconciliation) lives in the injectable `src/lib/chat-controller.js` with direct unit tests (MUNIDESK-533); `src/App.svelte` is down to ~1060 lines.
- VERIFIED 2026-07-26 (planner probe: full live run driven through a stubbed signed-in transport in headless Chromium, 1100×720 and 960×640) — the run lifecycle renders per spec end to end: the 17px milled ring with the mono `Routing` status pre-first-token, the steer/queue composer hint while a run is active, one coarse live-region announcement per phase, the measured streaming underline following the active line, the receipt collapsing and expanding into the mono record grid with route in signal, and §3.2’s Copy row. The submit-failure path keeps the draft, pairs the composer alert with the in-thread `Reply failed · Try again` record; interrupted runs offer Resume; the §10 first-run empty state matches spec with the composer focused. At the 960×640 minimum with the rail open nothing overflows; the grid holds the thread cell at its 320px minimum (content column 272px after the 24px gutters — as designed; noted, not selected).
- DONE 2026-07-26 — the sidebar current-thread row is a non-interactive current-thread record until the thread/branching contract lands (MUNIDESK-535). VERIFIED same day by planner probe of the rebuilt bundle: the row renders as a `div` with `aria-current="true"`, no tab stop, and the only sidebar tab stops are real actions (collapse, Home settings, profile).
- DONE 2026-07-26 — the voice gesture layer (press-and-hold, click, keyboard, and global-shortcut activation, hands-free double-activation promotion, and release timing) lives in the injectable `src/lib/voice-gesture.js` with direct tests (MUNIDESK-536). VERIFIED in the same probe: a pointer hold on the composer Voice button drives `dictation_start` through the built bundle. App.svelte's major extractions are complete.
- DONE 2026-07-26 — `src/lib/chat-transcript-controller.js` has direct unit tests over its copy epoch/timer lifecycle, receipt toggling, scroll-follow, and parallel-tool grouping, and a history reload resets stale parallel-tool groups alongside the expanded-receipts reset (MUNIDESK-538).
- DESIGN CALL 2026-07-26 (planner, §10 entitlement toast) — the event lane precedes the surface. Every session refresh funnels through `ensure_native_session` (`src-tauri/src/auth/mod.rs`), so change detection lives at that chokepoint: the app tracks the last `snapshot_version` observed this run in `AuthState`, and a refresh returning a different version emits one `entitlement-changed` Tauri event with a version-only redacted payload. The first snapshot after launch or sign-in is not a change, and sign-out resets the tracking. The surface stays per design-spec (edge states, line 181): one transient toast in ink with the fixed copy "Your access changed. Some models or connections may differ.", no signal accent — cut as its own UI slice once the lane lands, defining the design system's first toast primitive.
- DONE 2026-07-26 — the event lane landed (MUNIDESK-539): `AuthState` tracks the last observed `snapshot_version` across sync and async refresh paths, and a session refresh returning a different version emits one `entitlement-changed` event with a version-only payload; launch/sign-in first snapshots are not changes and sign-out resets tracking.
- VERIFIED 2026-07-26 (this wave's planner probe of the built bundle, stubbed signed-in `window.__TAURI__`, 1100×720) — the signed-in workspace still renders per spec (streaming underline on the active line only, provenance register, non-interactive current-thread record, composer registers), and the sidebar's Home-settings re-entry renders the §15 chooser surface correctly (eyebrow, current Home path, folder chips, Cancel / Confirm and continue). At the time of this probe, the built bundle had no frontend listener or consumer for `entitlement-changed`, confirming the lane/surface split. DONE — the selected toast surface slice has since landed: the design system's first toast primitive per `01-design-system.md` (bottom-center, `--surface`, hairline, `--radius-control`, ink, auto-dismiss 5s, never signal) consumes `entitlement-changed` with the fixed §2.6 copy through an injectable controller with direct tests. VERIFIED 2026-07-26 (this wave’s probe of the rebuilt bundle, stubbed signed-in `window.__TAURI__`, 1100×720): emitting `entitlement-changed` renders one bottom-center toast on `--surface` — hairline border, `--radius-control`, ink text, the fixed copy, `role=status` — rapid re-emission keeps a single element with a reset timer, it auto-dismisses inside the 5s window, and no signal color appears, matching 01-design-system.md’s toast primitive line for line.
- DESIGN CALL 2026-07-26 (planner, thread contract) — the thread/branching lane opens with a decision document, not code. The journal models runs only: `run_id` is the sole conversation identity, `run_workspaces` is the one grouping table, and the `thread_projection_*` tables are per-run redacted transcripts despite the name. Titles are derived per-read from the first prompt (`title_from`, `summaries.rs`), never stored; the attach protocol's `thread.list`/`thread.open` alias thread≡run over `RunSummary` (the desktop's own `DesktopAttachService.list_threads` still answers `unsupported_operation`); `chat_history` returns every subject-scoped run as one flat transcript; and `RunJournal::open` hard-fails on any `user_version` mismatch with no migration path, in a flat `runs.sqlite3` where ADR 0002 specified a profile-scoped `run-journal/v1/journal.sqlite3`. ADR 0002 constrains any design: mutable session rows are rejected — thread identity must be a durable event family, a run→thread stamping edge, or a rebuildable projection, never an authoritative mutable table. SELECTED — ADR 0016: thread identity over the run journal, deciding the identity mechanism, title/rename semantics, schema-version/migration stance (including the ADR 0002 path discrepancy), and the contract impact map (summaries/HMAC cursors, attach aliasing, `chat_history` scoping, run-start stamping, retention/delete/export cascade), plus naming the first implementation slice. UI slices (sidebar recents, `⌘N`, editable titlebar name, `⌘1–9`) follow the accepted ADR.
- DONE 2026-07-26 — ADR 0016 (thread identity over the run journal) is accepted: thread identity is an append-only, independently sequenced `thread.*` event ledger plus an immutable `run_threads(run_id, thread_id, thread_run_ordinal)` stamping edge written in the `run.started` transaction; rename is an event over a derived-title fallback; `PRAGMA user_version` becomes an ordered migration boundary; ADR 0002’s profile-scoped path stands, with the flat `runs.sqlite3` as a legacy migration source; and the contract impact map covers summaries/HMAC cursors, attach `thread.list`/`thread.open`, `chat_history`, run start, and the retention/delete/export cascade.
- DONE 2026-07-26 — ADR 0016 foundation sub-slice 1 (MUNIDESK-544): `RunJournal::open` resolves schema through ordered, per-step-transactional, validated migration steps keyed off `user_version` (the `MIGRATIONS`/`migrate_to_N` framework at schema v2); the post-open `CREATE TABLE IF NOT EXISTS` block, projection backfills, and versions seeding live in numbered steps; unknown newer versions stay fail-closed; a failed step rolls back to the pre-step state. Planner review 2026-07-26 of the merged diff confirmed the pragma ordering (WAL set before the migration transaction), validate-before-commit per step, and the four framework tests (fresh/shipped idempotent head migration, newer-version fail-closed without writes, missing-metadata fail without recreation, rollback on failed step).
- DONE 2026-07-27 — ADR 0016 foundation sub-slice 2 (MUNIDESK-546): the thread ledger, run stamp, and legacy backfill at schema v3. Migration step 3 creates the append-only `thread_events` ledger (stable event IDs, contiguous `thread_seq`) and the immutable `run_threads(run_id, thread_id, thread_run_ordinal)` stamping edge, and backfills every pre-existing run with its own UUIDv7 thread (one `thread.created` at seq 1 plus an ordinal-1 stamp) inside the step's transaction; `append_new_run` stamps each new run's fresh thread in the run-creation transaction; `validate_database` and `delete_run` extend to the new invariants. Planner review 2026-07-27 of the merged code confirmed the backfill query, both stamping paths, and the ledger validation sweep (canonical envelopes, UUIDv7 IDs, contiguous `thread_seq`, `thread.created` at seq 1, exactly one stamp per run, contiguous ordinals).
- DONE 2026-07-27 — ADR 0016 foundation sub-slice 3 (MUNIDESK-551): optimistic `expected_last_thread_seq` append APIs for `thread.title.renamed` (trimmed, non-empty, the shared 80-scalar `MAX_THREAD_TITLE_CHARS` bound) and the `thread.deleted` tombstone. Stale-sequence, unknown-thread, and post-tombstone appends fail without writes. Planner review 2026-07-27 of the merged append path confirmed the immediate-transaction tail re-check, the tombstone and unknown-thread rejections, and the shared title bound.
- DONE 2026-07-27 — ADR 0016 thread summaries (MUNIDESK-554): core `thread_summaries`/`workspace_thread_summaries` return one cursor-paginated summary per non-deleted thread, with `updated_at` as the maximum committed run or thread event time under the `thread_id` tiebreak, and the title from the latest rename else the bounded `title_from` fallback in stamp order. Planner review 2026-07-27 of the merged module confirmed bounded reads (one rename row or one first-prompt row per listed thread, never full-run envelope loads), tombstone exclusion inside the summary CTE, workspace scoping through `run_threads`, and the versioned scope-bound HMAC cursor whose snapshot boundary is re-checked on every later page. `run_summaries` and its consumers stayed untouched, as scoped.
- OBSERVED 2026-07-27 (planner measurement) — the summary query's `run_times` CTE takes `MAX(e.recorded_at)` over `run_threads JOIN events`, so every page reads every event row of every run, and streamed replies make `events` the fastest-growing table. On a replica schema holding 400 runs and 160,000 event rows, one 21-row page costs about 90ms, and `chat_thread_summaries_page` can repeat that query up to 100 times inside one command call. Reading each run's highest-`run_seq` event through the existing `events(run_id, run_seq)` index returns identical rows in about 2ms. SELECTED 2026-07-27 (this wave) — that rewrite. A run contributes its last event by `run_seq` rather than its largest timestamp string, because `run_seq` is the journal's own order and no append enforces a non-decreasing clock. No schema change: an index on `events(run_id, recorded_at)` would keep the old wording. The previously declined index on `events(run_id, recorded_at)` is now available to a later slice.
- DONE 2026-07-28 — that rewrite landed (MUNIDESK-584): the `run_times` CTE reads each run's highest-`run_seq` event through a correlated `ORDER BY e.run_seq DESC LIMIT 1` subquery over the `events(run_id, run_seq)` index, so a page no longer scans every event row. Planner review 2026-07-28 of the merged query confirmed the subquery, the unchanged summary shape, and the untouched tombstone and workspace-scope clauses.
- DONE 2026-07-27 — frontend code-health sweep (MUNIDESK-555): the `attach-pairing-requested` listener catches a rejected `attach_pairing_decide` invoke and `App.test.js` drives the event, `src/lib/thinking-transition.js` has a direct unit test, and the orphaned `onboardingCompleteState` export is resolved against its three inline literals.
- VERIFIED 2026-07-27 (probe: committed `test/probe/` fixtures over the rebuilt bundle in headless Chromium, 1100×720) — both fixture states render per spec. The empty workspace shows the §10 line with the composer focused. The restored history shows the user bubbles, tool cards, the `analysis/high → pi-2` receipt register with route in signal, and the interrupted-run `Reply interrupted. Resume` record. No new UI defect was found.
- DONE 2026-07-27 — ADR 0016 consumer switch, journal side (MUNIDESK-557): the `ThreadListService for RunJournal` impl in `src-tauri/core/src/attach/linux.rs` serves ledger thread identity. `thread.list` pages `workspace_thread_summaries`. `thread.open` resolves a ledger thread ID through `run_threads` in stamp order and pages the redacted projection with a cursor that binds thread, scope, and snapshot. The unconsumed `run_summaries`/`workspace_run_summaries` queries are deleted with their tests. Planner check 2026-07-27 of the merged code: `summaries.rs` is down to the shared `title_from` helper, no `run_summaries` reference remains outside the thread modules, and `cargo check --all-targets` on core reports zero warnings. `DesktopAttachService.list_threads` kept `unsupported_operation`, as scoped.
- DONE 2026-07-27 — code health (MUNIDESK-559): the Linux attach desktop service (`DesktopAttachService`, listener and approval state, `attach_pairing_decide`, `start_attach_listener`, credential persistence) lives in `src-tauri/src/attach_service.rs` with its tests (1,461 lines). `src-tauri/src/chat.rs` is down to 3,768 lines.
- DONE 2026-07-27 — ADR 0016 consumer switch, desktop side (MUNIDESK-562): `DesktopAttachService` serves `thread.list` and `thread.open` from the shared `ChatState` journal through the run-start boundaries seam, with pass-through workspace scoping and the core impl's error mapping. Planner review 2026-07-27 of the merged diff confirmed the boundary methods lock `ChatState.storage` and delegate to the core `ThreadListService` impl, and the adapter test pages two workspace-a threads while a workspace-b thread never appears.
- DONE 2026-07-27 — attach accept loop (MUNIDESK-563): `start_attach_listener` continues on per-connection accept results (`WrongUid`, `PeerCredentials`, transient `Accept` with a 50ms backoff) and stops only on `Closed`. Planner review 2026-07-27 confirmed the `should_retry_attach_accept` matrix and its four-case test, so one rejected local connection no longer ends the desktop accept loop for the session.
- OBSERVED 2026-07-27 — attach workspace namespaces diverge in production. Desktop runs stamp the cloud `grant.workspace` (`src-tauri/src/chat.rs:385`), while `desktop_attach_approval` (`src-tauri/src/attach_service.rs`) approves the desktop process working directory. The delegation slice therefore lists rows only when the two strings match, the same condition attach `run.start` already enforces in `configure_run`. The alignment is a design call for the ADR 0012 runtime-service lane, not a slice to improvise.
- DONE 2026-07-27 — ADR 0016 `chat_history` consumer switch, backend lane (MUNIDESK-565): typed `chat_thread_summaries` and `chat_thread_open` commands behind the signed-in gate serve cursor-paginated summaries and per-thread `HistoryEntry` pages over the shared journal. Planner review 2026-07-27 of the merged code confirmed the bounded ownership check (one stamped run ID plus one first envelope per listed thread), the 1–100 limit guard, and cursor pass-through to the core summary and stamp queries. Noted 2026-07-27: subject filtering runs after paging, so a page can hold fewer than `limit` owned summaries — empty in the worst case — while `next_cursor` stands. SELECTED this wave: the full-page fix (filter while paging, bounded scan) lands before the frontend switch consumes these pages.
- DONE 2026-07-27 — bounded launch reconcile (MUNIDESK-566): `reconcile_interrupted_runs` classifies terminality from the ordered event-type sequence through the new bounded `run_event_types` read (no envelope JSON) and fully reduces only candidate runs. Planner review 2026-07-27 confirmed the fail-open paths (unknown event type, non-contiguous `run_seq`) and the guard tests that assert agreement with `reduce().is_terminal()`. No schema change.
- DONE 2026-07-27 — ADR 0016 run-start stamping, journal side (MUNIDESK-573): `append_new_run_in_thread` creates a run and stamps it into an existing thread at max-plus-one. Unknown, tombstoned, and wrong-workspace threads are rejected before any write, inside the same immediate transaction that later inserts the event. `validate_thread_identity` now permits the deletion gaps the design call named legal. Planner review 2026-07-27 confirmed the pre-write rejections and the ordinal allocation.
- OBSERVED 2026-07-27 (same review) — the merged check tests ordinal positivity alone, so the design call's uniqueness invariant is unenforced in `validate_thread_identity`. A validation-only ticket is filed this wave.
- CORRECTED 2026-07-28 (planner) — the storage half of that observation was wrong. `run_threads` has carried `UNIQUE(thread_id, thread_run_ordinal)` in its table definition since MUNIDESK-546, so SQLite rejects a duplicate ordinal at insert time and no run could silently drop out of a thread page. MUNIDESK-578 added defense in depth over a closed hole rather than closing a live one. No unique index remains to add. `run_threads_thread_run` duplicates the implicit unique index instead, which the redundant-index note above covers.
- DONE 2026-07-27 — full `chat_thread_summaries` pages (MUNIDESK-574): subject filtering runs while paging, so pages fill to `limit` when enough owned summaries appear within the 100-core-page scan. At the scan bound, a short page returns the advanced core cursor so later owned summaries remain reachable. Planner review 2026-07-27 confirmed the bounded loop and cursor behavior.
- DONE 2026-07-27 — ADR 0016 run-start stamping, desktop side (MUNIDESK-580): `chat_submit` continues the desktop session's thread through the `SessionThread` tracker in `src-tauri/src/session_thread.rs`. A workspace or subject change starts a new thread. A rejected thread falls back to a fresh one without a second `run.started`. Attach-initiated runs keep one fresh thread per run. Planner review 2026-07-27 confirmed the fallback path and the ordinal-2 stamp test.
- DONE 2026-07-27 — the shell names the current thread from the first user prompt (MUNIDESK-581): `src/lib/thread-title.js` collapses whitespace, caps the title at 80 scalars with an ellipsis, and falls back to `New thread`. The titlebar and the sidebar row each truncate to one line and carry the full title on `title`. Planner render 2026-07-27 of the built bundle confirmed both surfaces.
- DONE 2026-07-27 — journal validation rejects duplicate per-thread run ordinals (MUNIDESK-578), so the ordinal that `thread_run_ids` pages and cursors on cannot silently drop a run from a thread page.
- DESIGN CALL 2026-07-27 (planner, thread continuity across launches) — `SessionThread` starts empty at launch, so the first prompt after a relaunch mints a new thread. The shell shows one thread row and has no new-thread control, so the desktop continues the newest thread the signed-in subject owns in the run's workspace and mints a fresh thread only when no candidate exists. That keeps today's single-conversation behavior. It also stops the coming frontend switch from restoring a transcript that the next run does not belong to. SELECTED 2026-07-27 (this wave) — that adoption slice.
- DONE 2026-07-28 — that adoption slice landed (MUNIDESK-583): the first prompt after a relaunch continues the newest thread the signed-in subject owns in the run's workspace through `newest_owned_workspace_thread`, and mints a fresh thread only when no candidate exists. Planner review 2026-07-28 confirmed the ownership check on each candidate thread's first stamped run and the fresh-thread fallback.
- DONE 2026-07-28 — that switch landed (MUNIDESK-588): `chat-controller.loadHistory` reads the newest summary from `chat_thread_summaries`, pages `chat_thread_open`, and `test/probe/stub.js` answers both commands. Planner check 2026-07-28 confirmed that no file under `src/` or `test/` names `chat_history` any more.
- DONE 2026-07-28 — that deletion landed (MUNIDESK-596): `chat_history` and `history_entries` are gone from `src-tauri/src/chat.rs` and from the command list in `src-tauri/src/main.rs`, and their attachment-redaction, pending-permission, and subject-filter tests now run against the thread commands. Planner check 2026-07-28 found no live `chat_history` command reference. The profile-scoped path migration still follows.
- DONE 2026-07-28 — that extraction landed (MUNIDESK-599): the thread read and selection commands live in `src-tauri/src/chat_threads.rs` (1,168 lines) with their tests, and `src-tauri/src/chat.rs` is down to 4,213 lines. The next thread slices (rename over `thread.title.renamed`, delete over `thread.deleted`) land in the new module.
- OBSERVED 2026-07-29 (planner, code-health lens) — `src-tauri/src/chat.rs` is back to 4,224 lines and remains the largest file in the repository. One file holds the Tauri command surface, run-start preparation, and the whole run coordinate loop. `coordinate` runs from line 1267 to line 1881, and its private helpers run to about line 2154. A grep over `src-tauri/src` finds no caller outside `chat.rs` for `coordinate`, `coordinate_extension_ui_request`, `coordinate_permission_answer`, `tool_journal_entry`, `permission_journal_payload`, `close_open_effects`, `append_terminal`, `fail_with_open_effects`, `append_emit`, `chat_event`, and `fail`. `chat_tool_activity` and `event_envelope` do have callers in `chat_threads.rs` and `test_support.rs`, so they stay. SELECTED 2026-07-29 — that extraction, following MUNIDESK-559 and MUNIDESK-599.
- DONE 2026-07-29 — that extraction landed (MUNIDESK-628). The run coordinate loop and its private helpers live in `src-tauri/src/chat_coordinate.rs` (1,194 lines). `src-tauri/src/chat.rs` is down to 3,065 lines, and `src-tauri/src/model_install.rs` is now the second largest file at 2,089 lines. Planner check 2026-07-29 confirmed `chat.rs` no longer holds `coordinate`, and that `chat_tool_activity` and `event_envelope` stayed behind for their callers in `chat_threads.rs` and `test_support.rs`.
- OBSERVED 2026-07-28 (planner) — `chat_thread_summaries` pages every thread the subject owns, while run start adopts the newest owned thread in the run's workspace. The two name the same thread while the desktop process keeps one working directory, and they can diverge when it does not. The 2026-07-27 attach note above records the same namespace split. Exposing workspace scope on the read commands waits on that ADR 0012 design call.
- DESIGN CALL 2026-07-28 (planner, thread selection) — the sidebar heading reads `Threads`, the journal holds every thread the signed-in subject owns, and the shell reaches exactly one of them. A recents list is dishonest until selection has a seam, because the next prompt would still continue the newest owned thread. `SessionThread` therefore models three states instead of one tracked thread. Unset keeps today's adopt-the-newest-owned behavior. Selected names a thread the user picked. Fresh forces the next run to mint a new thread. A selection carries no workspace, because the workspace arrives with the chat grant at run start, so `append_new_run_in_thread` validates it and the existing fresh-thread fallback covers a mismatch. DONE 2026-07-28 — that seam landed (MUNIDESK-595): `SessionThread` holds `Unset`, `Selected`, or `Fresh`, and the `chat_select_thread` and `chat_new_thread` commands write the last two behind the signed-in gate and the ownership check. Planner review 2026-07-28 confirmed the subject match on every offered choice and the ownership check before a selection is stored.
- DONE 2026-07-28 — that list landed (MUNIDESK-598): `loadHistory` reads one 20-thread summary page, the sidebar renders a row per thread with its title and relative time, and a click opens the chosen thread through `chat_select_thread` and `chat_thread_open`. Planner render 2026-07-28 of the built bundle at 1100×720 and at the 960×640 minimum confirmed the rows, the current-thread record, the ellipsized long title, and the scrolling list.
- DONE 2026-07-28 — the New thread control landed (MUNIDESK-601): the sidebar action calls `chat_new_thread` under the platform chord, clears the transcript, and returns focus to the composer, so the command no longer sits unreached.
- DONE 2026-07-28 — the year-bearing label and the shared dot slot landed (MUNIDESK-604).
- DONE 2026-07-28 — the single-pass thread read landed (MUNIDESK-603).
- DONE 2026-07-28 — thread rename reached the journal (MUNIDESK-602): `chat_rename_thread` appends `thread.title.renamed` at the current tail after the ownership check, and `RunJournal::last_thread_seq` reads that tail. Planner review 2026-07-28 of the merged command confirmed the `subject_owns_first_run` gate before any write and the four rejection cases in `rename_thread_accepts_owned_thread_and_rejects_invalid_requests`. The command has no frontend caller.
- DONE 2026-07-28 — that rename surface landed (MUNIDESK-609): the titlebar name reads the selected thread's stored summary title, opens an inline field on activation, and commits through `chat_rename_thread`. Planner render 2026-07-28 of the rebuilt bundle confirmed the titlebar name renders as an activatable control in every fixture state.
- DONE 2026-07-28 — thread delete reached the journal (MUNIDESK-618): `chat_delete_thread` (`src-tauri/src/chat_threads.rs:390`) mirrors `rename_thread` with the ownership check, the current-tail read, and one `thread.deleted` append. `delete_thread` then calls `SessionThread::fresh_if_current`, so deleting the open thread moves the tracker to `Fresh`. Planner check 2026-07-29 confirmed the command carries no frontend caller.
- OBSERVED 2026-07-29 (planner probe, driven through the built bundle) — the sidebar exposes no thread action at all. A scripted read of the restored-history fixture reports one `div` row and two `button` rows, each holding zero nested controls, so a user cannot delete a conversation from the shell. `docs/spec/02-desktop-app.md` §2.1 asks for a row context menu holding rename, share to project, and delete. That menu primitive still does not exist, and rename already shipped as an inline titlebar control rather than waiting for it.
- DESIGN CALL 2026-07-29 (planner, destructive row action) — delete follows §3.2's quiet action row, not a menu. The control hides at rest, appears on row hover or focus, and keeps its tab stop, the way `.message-actions` does at `src/App.svelte:1086`. Activation opens an inline confirm inside the row, because the tombstone has no undelete command and the design system has no dialog primitive. Current practice on destructive actions matches that shape: match the friction to the blast radius, keep modals rare so the rare one still reads, and prefer an inline confirm for a single-object delete. The confirm stays in ink and words, because `docs/spec/01-design-system.md` line 54 makes `oxide` an admin-record color and never alarm chrome. SELECTED 2026-07-29 — that delete surface.
- DONE 2026-07-29 — that delete surface landed (MUNIDESK-627). Each sidebar thread row carries a `Delete` control that sits at `opacity: 0` until row hover or focus (`src/App.svelte:1050`). Activation swaps the row for an inline confirm holding `Delete` beside `Cancel`, and Escape cancels and returns focus to the delete control. `chat-controller.deleteThread` clears the transcript when the deleted thread is the open one. Planner probe 2026-07-29 of the rebuilt bundle confirmed the reveal on hover, the enabled control on the current row, and the inline confirm. That confirm truncates a long title hard inside the 260px sidebar, and the control's `aria-label` carries the full title, so no follow-up is selected.
- DONE 2026-07-28 — the hard-coded `local · durable` literal is gone from the titlebar (MUNIDESK-619). Planner render 2026-07-29 of the rebuilt bundle confirms the titlebar row now carries the session name and the artifact toggle alone. The `⌘K` palette hint still waits on the palette itself.
- OBSERVED 2026-07-28 (planner probe, driven through the built bundle) — the sidebar loses the thread the user is in. From the restored-history fixture the probe clicked New thread and sent one prompt. Before the prompt the placeholder row renders and carries `aria-current`. After it the placeholder disappears, because it renders only while `messages.length === 0`, and no row carries `aria-current` at all: the three real rows stay as they were, and the new thread never joins the list, because `loadHistory` reads `chat_thread_summaries` once at launch. The titlebar names the new thread correctly, so the sidebar contradicts the titlebar one region away. SELECTED 2026-07-28 (this wave) — the current-thread refresh. The desktop already knows the answer: `prepare_opened_run` (`src-tauri/src/chat.rs:1007`) calls `SessionThread::record` (`src-tauri/src/chat.rs:1008`) with the stamped thread ID at run start, and nothing reads it back.
- DONE 2026-07-28 — that refresh landed (MUNIDESK-608): the new `chat_current_thread` command reads the session's stamped thread back out of `SessionThread`, and `chat-controller.refreshThreads` reads it beside the summary page, so the sidebar marks the thread the user is in. Planner render 2026-07-28 of the rebuilt bundle confirmed the restored-history list.
- OBSERVED 2026-07-28 (planner, code-health lens) — three registered Tauri commands have no caller anywhere in the repository. `gemma_install_start`, `gemma_install_status`, and `gemma_install_cancel` (`src-tauri/src/model_install.rs:1013-1039`) each carry the comment "Keep this legacy model-specific command name for frontend wire compatibility", and no file under `src/`, `test/`, or `docs/` names them. Onboarding reads `required_model_acquisition_status` instead, and `main.rs:35` starts the required-model acquisition itself. The names also contradict ADR 0017, which pins a Qwen artifact. DONE 2026-07-28 — that deletion landed (MUNIDESK-610). `parakeet_install_start`/`_status`/`_cancel` are unreferenced the same way, and they stay because they are the only trigger for ASR artifact acquisition: whether dictation acquires its models on demand is a design call, not a deletion.
- OBSERVED 2026-07-28 (planner, information-design lens) — `loadHistory` asks `chat_thread_summaries` for 20 threads and drops the `nextCursor` the command returns, so a subject with more than 20 threads cannot reach the twenty-first from the shell. Search (`⌘F`) is deferred, so the list is the only path. Parked again 2026-07-28: the sidebar list took four slices in three waves and rests until a wave passes without touching it.
- SELECTED 2026-07-31 (this wave) — that reach, now that three waves have passed with no sidebar list slice. Planner read 2026-07-31 confirmed both drop sites, `src/lib/chat-controller.js:58` and `:116`, and confirmed that `chat_thread_summaries` (`src-tauri/src/chat_threads.rs:328`) already takes a `cursor` argument. The list gains an explicit control at its end rather than scroll-driven loading. Current accessibility guidance records that infinite scroll strands keyboard and screen-reader users, and `docs/journal-ideas/tests-without-layout-cannot-see-the-fold.md` records that the frontend suite runs with no layout engine, so the control is both the reachable pattern and the testable one.
- DONE 2026-07-31 — that reach landed (MUNIDESK-712). `chat-controller.js` keeps the summary cursor, the sidebar renders an `Older threads` button at the end of the list, and each activation appends the next page and moves focus to its first new row. Planner render 2026-07-31 of the built bundle at 1100x720 confirmed the control below the three fixture rows.
- OBSERVED 2026-07-28 (planner, database lens) — `thread_projection_run_order` on `thread_projection_entries(run_id, ordinal)` duplicates the implicit index that `PRIMARY KEY(run_id, ordinal)` already creates on the same columns in the same order, the third instance of the MUNIDESK-589 shape. That table grows with every streamed reply, so each chunked row pays for a second identical b-tree entry. Parked again 2026-07-28: this drop belongs in one slice beside the two parked ones that `validate_thread_schema` asserts, and that slice edits the schema validator and two corruption tests together.
- OBSERVED 2026-07-28 (planner, code read) — `chat_thread_open_page` projects each run through `project_history_entry`, which loads every envelope of that run and reduces it. A 20-run page of streamed replies therefore reads every `text.delta` row of 20 runs, and the frontend switch above puts that read on the launch path. Bounded projections exist already (`thread_projection_versions`, `receipt_projection`, `permission_pending_projection`). Not selected this wave: deciding which fields those projections own is a contract call, not a query rewrite. Planner code read 2026-07-28 narrowed the gap: `thread_projection_entries` already accumulates the assistant text in chunked rows, `receipt_projection` holds the receipt, and `permission_pending_projection` holds the gate, so only `tool_activity`, `attachments`, `phase`, and `resumable` still force the full envelope load.
- MEASURED 2026-07-29 (planner, first-run layout lens) — the onboarding surface outgrows the window on its import path, and its actions leave the screen with it. `main` is `min-height: 100vh; display: grid; place-content: center` (`src/App.svelte:934`) with no scroll region, so the whole surface grows the document instead. Driven through the built bundle at the shipped 960x640 window minimum, the archive-review screen puts `Continue with selected`, `Continue without importing`, and `Choose a different ZIP...` at y 685 to 717, entirely below the 640px fold. The pre-triage screen with 24 approved sources runs to 1,079px and puts `Generate local proposal` 375px below the fold, because `.triage-sources` carries no height bound while `.manifest` carries `max-height: min(42vh, 360px)`. The manifest list is also its own scroll region, so a pointer over the list scrolls the list before it scrolls the page. At the 1100x720 default both screens fail the same way. DONE 2026-07-29 — that fit slice landed (MUNIDESK-657). The onboarding surface is a fixed header, one scrolling content region, and a fixed footer, in the `src/lib/AccessPanel.svelte` idiom. Planner render 2026-07-30 of the built bundle at 1100x720 confirmed the archive-review screen keeps `Choose a different ZIP…`, `Continue without importing`, and `Continue with selected` on screen while the manifest scrolls above them.
- OBSERVED 2026-07-29 (planner, code-health lens, verified line by line) — four Tauri event-listener registrations in the frontend drop a rejected registration promise. `createChatController.start` awaits `listen('chat-event', ...)` with no guard (`src/lib/chat-controller.js:86`), and `src/App.svelte:547` calls it without awaiting it and without a `.catch`. `createEntitlementToast.start` repeats the shape (`src/lib/entitlement-toast.js:29`), called at `src/App.svelte:548`. The `attach-pairing-requested` registration chains `.then` with no `.catch` (`src/App.svelte:515`), which is the half of MUNIDESK-555 that stayed open: that fix guarded the callback body and left the registration itself bare. The `onDragDropEvent` registration repeats it (`src/App.svelte:592`). The `chat-event` case carries the real consequence: a rejected registration leaves the run pinned in its thinking phase forever, because no streamed delta, tool update, or terminal phase can ever arrive, and the user sees no error. `dictation-controller.js` and `voice-shortcut.js` are the guarded counterexamples, because `listenForDictation` runs inside `start()`'s try/catch. DONE 2026-07-29 — that guard landed (MUNIDESK-675). `createChatController.start` catches a rejected `listen`, records the failure, reports `Live replies cannot arrive. Try again.`, and retries the registration on the next history load. `createEntitlementToast.start`, the `attach-pairing-requested` registration, and the `onDragDropEvent` registration each carry a catch. Planner review 2026-07-30 confirmed all four guarded sites and the retry path.
- OBSERVED 2026-07-29 (planner, first-run correctness lens) — first-run folder setup does not fail open. `home_status` returns an error whenever the platform Documents folder is unavailable, and the onboarding surface then offers `Try again` alone. The Desktop QA automation section below carries the measurement, the fallback design call, and the two selected slices.
- MEASURED 2026-07-30 (planner, drove the built bundle through the first-run import path) — the approved-files path dead-ends. The probe confirmed a Home, chose a ZIP, checked all three manifest entries, and clicked `Continue with selected`. The screen that follows is headed `Create your local proposal`. It says `Generate a local proposal from 3 approved files. You will review it before anything can be imported.`, lists the three approved sources, and then offers `Back to archive review` beside the line `This import path is unavailable.` A scripted read reports exactly one button on that screen. A user therefore gives consent, previews the archive, selects entries, and extracts them, and then reaches a wall that contradicts its own heading.
- MEASURED 2026-07-30 (same read) — every state past that wall is unreachable. `generateTriageReport` is deleted, so nothing produces `triage-review`, and `triage-confirmed`, `triage-saving`, and `triage-invalid` follow it out of reach. `home_confirm_import`, `compile_onboarding_home_write_plan`, `persist_onboarding_home_write_plan`, and `OnboardingTriageReport` all remain in the tree with no reachable caller. `src/App.test.js:1130` now asserts the wall as intended behavior.
- DESIGN CALL 2026-07-30 (planner, the import end state) — the 2026-07-29 ruling keeps import consent, preview, provenance, and verbatim originals, and it removes the AI proposal alone. The import therefore ends by saving the approved verbatim originals into the Home. The write plan drops the `memory/onboarding-report-<date>.md` document and the `agents/agent-*.md` documents. It keeps one `memory/imports/<date>-<name>-<digest>.md` document per approved entry, with the provenance frontmatter and the verbatim body unchanged. Every bound, slug, digest, collision, and rollback rule stays as written, because none of them depended on the report.
- DONE 2026-07-30 — that end state landed (MUNIDESK-682). `compile_onboarding_home_write_plan` (`src-tauri/core/src/home.rs:471`) now emits one `memory/imports/<date>-<name>-<digest>.md` document per approved entry and nothing else, and the report and starter-agent documents are gone with their tests. The `approved-review` screen is headed `Save approved files`, lists the approved sources with their provenance, carries the Home path row with `Choose folder…`, and ends on `Save Home and finish`. `test/probe/approved-files.html` drives the built bundle through consent, preview, selection, and extraction to reach it. Planner render 2026-07-30 at 1100x720 confirmed the screen and its primary action on screen.
- NOTED 2026-07-30 (planner, same render) — two small things stay unselected. The `approved-review` screen offers no way back to archive review, because `returnToArchiveReview` is reachable from `import-invalid` alone, so a user cannot revise the selection at the last step. The path card is labelled `Muniment Home` there and `Home location` on the picker screen, which names one thing two ways. Both wait, because the import screens shipped this wave and rest one wave.
- SELECTED 2026-07-30 (the next wave) — both of them, now that the wave has passed. The `approved-review` footer gains the `Back to archive review` control that the `import-invalid` footer already carries (`src/lib/Onboarding.svelte:203`), and the path row reads `Home location` on both screens. Planner renders 2026-07-30 of `onboarding.html` and `approved-files.html` at 1100x720 confirmed the two labels side by side and the single action on the last screen.
- DONE 2026-07-30 — both landed (MUNIDESK-692). The `approved-review` footer carries `Back to archive review` beside `Save Home and finish`, and the path row reads `Home location` on the picker screen and on the last screen. Planner render 2026-07-30 of `approved-files.html` at 1100x720 confirmed both on screen.
- MEASURED 2026-07-30 (planner, information-architecture lens, scripted read of the built bundle at 1100x720) — the signed-in workspace exposes no heading and no named landmark. A query for `h1`, `h2`, `h3`, and `h4` over the restored-history fixture returns zero elements, and the same query over the empty fixture returns zero. `main`, the `.workspace` section, the `titlebar` header, and the `sidebar` aside all render with no accessible name. `Threads` is a `<p class="side-label">` (`src/App.svelte:654`), so it labels nothing. The thread rows sit in a plain `<div class="thread-list">` (`src/App.svelte:655`), so a screen reader announces neither the group nor the row count. The transcript is a bare `<div class="thread">` (`src/App.svelte:688`) with no role and no name. The one h2 in the repository belongs to the artifact rail, and it renders only while the rail is open. A user driving this shell by heading or landmark reaches nothing.
- DESIGN CALL 2026-07-30 (planner, the shell heading register) — the open conversation is the workspace's one `h1`, and it is the titlebar name that already carries the thread title. `Threads` and the rail's `Artifacts` are the two `h2` groups beside it. The rename field replaces that heading while a rename is open, and it keeps its own `Thread name` label, so no heading wraps a text input. The thread rows become a real list, because the count and the position are information the sidebar shows visually and withholds otherwise. Nothing changes on screen: the heading takes the register its current class already sets, and `src/styles/type-scale.test.js` still permits a component font size only through a `var(--text-*)` token. SELECTED 2026-07-30 (this wave) — that structure.
- DONE 2026-07-30 — that structure landed (MUNIDESK-687). The workspace carries one `h1`, `Threads` and `Artifacts` are its two `h2` groups, and the thread rows are a real list. Planner render 2026-07-30 of the rebuilt bundle confirmed the shell looks unchanged.
- MEASURED 2026-07-30 (planner, drove a Markdown-bearing reply through the built bundle at 1100x720) — the thread renders raw Markdown source. `src/App.svelte:727` writes the reply into `<p class="response-prose">{message.run.text}</p>`, `.response-prose` (`src/App.svelte:1072`) sets `white-space: pre-wrap` alone, and no file under `src/` parses Markdown. A reply carrying a `##` heading, `-` bullets, `**` emphasis, inline backticks, a fenced code block, and a pipe table rendered every marker verbatim in the sans-serif prose register. Pi writes Markdown, so the app's central element shows source characters to the user.
- SELECTED 2026-07-30 (this wave) — the decision document rather than the renderer. Three constraints make this an ADR: ADR 0018 makes model output untrusted content and the Tauri CSP is only the backup defense, `src/lib/streaming-underline.js` measures the last line box of one paragraph and block-level Markdown changes that geometry, and ADR 0020 already picked `diff2html` for code diffs and forbids adding React. The document follows the ADR 0020 format, measures candidates against named bars, and names its first implementation slice.
- DONE 2026-07-30 — that document landed as ADR 0023 (MUNIDESK-699). The desktop parses with `marked` and sanitizes with DOMPurify. Headings map to `h2` through `h5`, and every construct outside the subset renders as literal source text. An image creates no `img` element, and a link resolves for `https:`, `http:`, and `mailto:` alone. Markdown renders after the terminal run event, so `src/lib/streaming-underline.js` keeps measuring plain text. Planner check 2026-07-30 against the npm registry confirmed both pins are real: `marked` 18.0.7 is the current release, and `dompurify` 3.4.12 published on 2026-07-11.
- SEQUENCED 2026-07-30 (planner) — the ADR's first slice adds the pins, the pure module, the Svelte component, a token stylesheet, and about fourteen test cases. The module carries every security rule, and the component carries the visual register. Those are two concerns, so they split. SELECTED 2026-07-30 (this wave) — the pins and the module. The component and the `src/App.svelte` wiring follow it.
- DONE 2026-07-31 — that slice landed (MUNIDESK-702). `src/lib/assistant-markdown.js` parses with the pinned `marked` 18.0.7, sanitizes with the pinned `dompurify` 3.4.12, renders every construct outside the subset as literal source text, and returns the complete original reply as plain text whenever the sanitizer removes anything. Planner check 2026-07-31 confirmed both pins in `package.json` and the module's own test file.
- MEASURED 2026-07-31 (planner, drove a live reply through the built bundle at 1100x720) — the thread still shows source characters, because no component consumes that module. A completed reply carrying a `##` heading, `-` bullets, `**` emphasis, inline backticks, a fenced block, a pipe table, and a link rendered every marker verbatim, and `.response-prose` reported an `innerHTML` identical to the raw reply text.
- SELECTED 2026-07-31 (this wave) — the component, its token stylesheet, and the `src/App.svelte` wiring. Current practice adds one bar that the ADR did not measure. A GFM table inside a narrow assistant column overflows, and both the VS Code chat view and the Codex terminal renderer answer that inside the table rather than by scrolling the conversation. The thread column drops to 272px at the 960x640 window minimum with the rail open, so the table and the fenced block each scroll inside their own block. Sources: `github.com/microsoft/vscode/issues/265062` and `github.com/openai/codex/pull/24636`.
- DONE 2026-07-31 — that slice landed (MUNIDESK-706). `src/lib/AssistantMarkdown.svelte` renders the sanitized reply through the pure module, its token stylesheet resolves every heading through the `--text-*` scale, and a fenced block and a GFM table each scroll inside their own block. `test/probe/markdown.html` is the fixture. Planner render 2026-07-31 of the built bundle at 1100x720 and again at the 960x640 minimum confirmed the heading, the bullets, the inline code, the fenced block, and the table, with the thread never scrolling sideways.
- MEASURED 2026-07-31 (planner, read the rendered anchor against the desktop's registered plugins) — an assistant link has no defined click behavior. `src/lib/assistant-markdown.js:78` emits `target="_blank" rel="noopener noreferrer"`, and the Markdown fixture renders that anchor. No file under `src/` or `src-tauri/src/` handles a click on it. `src-tauri/src/main.rs:22-24` registers the dialog, global-shortcut, and window-state plugins alone, and the `default-src 'self'` CSP in `src-tauri/tauri.conf.json` does not govern top-level navigation. The outcome therefore depends on the platform webview rather than on a decision in this repository, and a webview that follows the link replaces the signed-in workspace with a model-supplied page in a window carrying no address bar and no back control. ADR 0018 makes model output untrusted content.
- SELECTED 2026-07-31 (this wave) — the explicit answer. Tauri documents the opener plugin as the way to open a URL in the default program, and `tauri-plugin-opener` and `@tauri-apps/plugin-opener` both publish 2.5.4, dated 2026-05-02. The capability scope allows `https:`, `http:`, and `mailto:` alone, which matches the three protocols the Markdown module already approves.
- DONE 2026-07-31 — that answer landed (MUNIDESK-711). `src/lib/external-link.js` holds `createExternalLinkHandler`, which cancels the native activation, reads the anchor's own `href`, and opens it only for `https:`, `http:`, and `mailto:`. `src/lib/AssistantMarkdown.svelte` delegates every anchor click to it, `src-tauri/src/main.rs` registers the opener plugin, and `src-tauri/capabilities/default.json` carries the matching scope. Planner read 2026-07-31 confirmed the `currentTarget.contains` guard that keeps the handler off anchors outside the reply.
- MEASURED 2026-07-30 (planner, same drive) — one unbreakable token scrolls the thread sideways. A reply holding a 200-character unbroken digest made `.response-prose` report `scrollWidth` 1550 against `clientWidth` 600 at the 960x640 window minimum, and `.thread` report `scrollWidth` 1550 against `clientWidth` 652, so the rest of the digest needed a sideways scroll. The computed `overflow-wrap` on `.response-prose` is `normal`, while `.user-message` (`src/App.svelte:1065`), `.tool-name` (`:1083`), `.permission-card p` (`:1090`), and `.receipt-record dd` (`:1106`) each declare `overflow-wrap: anywhere` already. `.provenance` (`:1100`) renders server-supplied route, model, and capability strings and carries no wrap rule either. SELECTED 2026-07-30 (this wave) — that wrap rule, its style lint, and the one-line `DESIGN.md` record.
- DONE 2026-07-30 — that wrap rule landed (MUNIDESK-700). `.response-prose` and `.provenance` each wrap an unbreakable string. `src/styles/text-wrap.test.js` lints six text-bearing selectors in the `signal-allowlist.test.js` idiom, and `DESIGN.md` carries the one-line record.
- OBSERVED 2026-07-30 (planner, code-health lens) — two symbols outlive their consumers. `auth_ensure_fresh` (`src-tauri/src/auth/mod.rs:211`) is a registered Tauri command that no file under `src/`, `test/`, or `docs/` names, and `auth_entitlement_snapshot`, `auth_devices`, and `fresh_tokens_async` each call `ensure_native_session` themselves. `dictationTransforms` (`src/lib/dictation-state.js:4`) still lists the four post-dictation transforms, and the `dictation_polish` command left with the resident model, so `src/App.test.js` now asserts that the frontend never invokes it. Not selected this wave, because three slices already fill it.
- SELECTED 2026-07-31 (this wave) — that deletion. Planner check 2026-07-31 confirmed both symbols again: `auth_ensure_fresh` appears at `src-tauri/src/auth/mod.rs:211`, in the command list at `src-tauri/src/main.rs:44`, and in three `docs/auth.md` lines, and nowhere else. `dictationTransforms` appears at `src/lib/dictation-state.js:4` and in its own test alone. The stale `dictation_polish` stub at `src/lib/dictation-controller.test.js:14` leaves with it, and `test/smoke.sh` already forbids that command name outside test files.
- DONE 2026-07-31 — that deletion landed (MUNIDESK-708). `auth_ensure_fresh` is gone from `src-tauri/src/auth/mod.rs`, from the command list in `src-tauri/src/main.rs`, and from `docs/auth.md`. `dictationTransforms` and the stale `dictation_polish` stub are gone with it.
- MEASURED 2026-07-30 (planner, drove a failed tool through the built bundle at 960x640) — the failed tool card names one state twice, and the screen disagrees with the screen reader. `src/App.svelte:1087` sets `.tool-failed .tool-status::before { content: 'error · '; }`, so the row reads `error · failed`. The row's `aria-label` (`src/App.svelte:783`) reads `Read contract.pdf: failed`, so a screen reader hears the state once and never hears the generated word. The failed status also computes the same muted `rgb(92, 100, 97)` as a completed status, so the word carries the whole state. A scripted read found a fourth gap beside it: each parallel group row (`src/App.svelte:775`) is a `div` with no role, and ARIA drops an `aria-label` on a role-less generic element.
- DESIGN CALL 2026-07-30 (planner, the failed tool record) — the row says `failed` once, in the mono status slot, and it takes `--oxide`. `docs/spec/01-design-system.md` §6 defines the running and complete states alone. The color law keeps `--signal` for the running header, and line 54 makes `oxide` a static admin-record color rather than alarm chrome. `src/lib/AccessPanel.svelte:253` already renders the revoked device as an `--oxide` record, so this row follows a shipped precedent. The word stays the carrier, per the §8 accessibility floor. SELECTED 2026-07-30 (this wave) — that record, plus the accessible name that matches the visible text.
- DONE 2026-07-31 — that record landed (MUNIDESK-704). `.tool-failed .tool-status` takes `--oxide`, the `error · ` pseudo-element is gone, and every tool row's accessible name reads the visible words in the visible order. Planner read 2026-07-31 of the merged diff confirmed the `role="status"` that each parallel-group row gained, so ARIA no longer drops those names.
- OBSERVED 2026-07-30 (planner, same drive) — the sidebar time label wraps. A row whose relative time reads `Just now` breaks that label across two lines and grows the row from 36.1px to 40px, so the current row stands taller than every row beneath it. Parked: the sidebar took the delete control on 2026-07-29 and the list markup on 2026-07-30, so it rests this wave. CORRECTED 2026-07-31 (planner, measured the same rows again) — that wrap is gone. A scripted render at 1100x720 with the three fixture rows relabelled `Just now`, `Jul 11`, and `Feb 3, 2025` reports every row at 36.1px and every time label on one 12px line. The shared dot slot from MUNIDESK-604 and the list markup from MUNIDESK-687 closed it, so no ticket is filed.
- OBSERVED, NOT YET SELECTED — `--space-*` tokens are absent, and a spacing sweep would touch nearly every declaration in the app, so it stays parked rather than half-done. The titlebar has no `⌘K` hint, and should not get one while the palette itself is deferred. The full §1.8 ring behavior engine (decorrelated breath/spin/trace, flexing milling depth) is explicitly gated on a ≥34px thinking surface per the 2026-07-26 design call.
- No further artifact-rail content slice is selected. Artifact data, rendering, persistence, sharing, and cloud contracts remain deferred to Phase 4 item 19; select the first real-contract slice only when its authoritative event/data boundary is specified. CLI/VS Code defaults wait for workspace memory semantics.
- Open items: headless/server CLI stance owner ruling; final model promotion waits on MUNIQA routing eval; Gemma 4 E2B remains a future owner-gated contingency pending llama.cpp PLE support. MUNICLOUD model artifact proxy/redirect is a ripple.

## Desktop QA automation
- RATIFIED — layered frontend browser coverage plus installed-nightly real-app validation on serialized pve01 desktop-ci VMs; Windows/Linux use WebdriverIO, macOS install/launch smoke plus screendumps/manual owner pass.
- DONE — ADR 0013; canonical runner contract; Linux `.deb` real-sign-in smoke; Windows MSI real-sign-in smoke; macOS install/launch smoke; stable Linux/Windows JUnit artifacts.
- DONE 2026-07-22 — installed Linux `.deb` and Windows MSI each submit one unique prompt through authenticated production chat and verify a non-empty assistant turn plus server receipt route under a bounded deadline; rendered production conversations are excluded from uploaded screenshots.
- DONE 2026-07-22 — a failed installed-nightly platform job opens or updates one SHA-scoped triage issue containing metadata and links to the private redacted diagnostics.
- DONE 2026-07-23 — installed Linux and Windows nightly validation each attach a generated-from-source PNG, submit it through authenticated production chat, and require the model to return the image-only token; fixtures remain outside uploaded diagnostics. Test one pinned finalized installer SHA; no mocked production paths. Signing/notarization and web/API suite remain out of scope.
- OBSERVED 2026-07-25 — the signed-in-frame defect above survived every layer of this suite, because the nightly screenshots deliberately exclude rendered production conversations and the frontend suite asserts presence rather than absence.
- DECIDED 2026-07-26 — do not add a headless browser to the CI gate for it. The smoke job runs on the self-hosted runner with no browser installed, and the whole MUNIDESK-508 shape (a class in markup that no rule anywhere defines) is statically detectable, so the class-usage lint selected above buys the same coverage inside the existing vitest job with no new infra and no flake. Planning-time product inspection keeps rendering the built bundle against a stubbed signed-in `window.__TAURI__` in headless Chromium; that is a planner tool, deliberately not a CI layer.
- OBSERVED 2026-07-26 — the clipped-popover defect above is invisible to both existing layers, for the mirror image of the 2026-07-25 reason: jsdom has no layout, so three tests in `src/App.test.js` can click a `Sign out` button that never renders on screen, and the nightly screenshots exclude the signed-in surfaces it lives on. The 2026-07-26 decision stands — no browser in the CI gate — so the ticket carries a structural assertion (the action lives outside the scrolling region, in the existing style-block-parsing idiom) rather than a geometric one, and planning-time rendering stays the layer that finds this class of defect.
- VERIFIED 2026-07-26 — that split worked as designed: the popover restructure landed with its structural assertions, and the planner's stubbed-Chromium render confirmed the geometric outcome (popover inside 720px, Sign out on screen) the CI layers deliberately cannot see.
- OBSERVED 2026-07-26 — the dead-entry-chord defect above was invisible to the frontend suite for a third reason in the series: App.test.js dispatches its shortcut keydowns from the non-editable `document` target rather than the entry-focused composer, so `isEditableTarget(event.target)` returns false and the guard that kills the chords in the real entry state is never exercised. The fix’s tests dispatch from the focused composer.
- VERIFIED 2026-07-26 (planner probe) — the stubbed-transport render now also drives full run lifecycles (submit, streamed deltas, completion receipts, failure, interrupt/resume) through the built bundle by emitting `chat-event` payloads into the stubbed listener, extending planning-time inspection past static states to the interactive contract the CI layers cannot see.
- DONE 2026-07-27 — the installed Linux E2E harness starts tauri-driver on port 4444 again, so WebdriverIO sessions are created (MUNIDESK-550).
- DONE 2026-07-27 — the planner browser probe harness lives under `test/probe/` (MUNIDESK-552): the stubbed signed-in `window.__TAURI__` page over the built bundle, `npm run probe`, and empty plus restored-history fixtures. VERIFIED same day on its first in-tree run from a clean clone: `npm run build` plus the probe pages render both fixture states per spec at 1100×720, including the provenance register, tool cards, and the interrupted-run Resume record. Planning-time inspection now starts from a committed harness instead of a hand-built stub, and it stays a planner/dev tool rather than a CI layer, per the 2026-07-26 decision.
- DONE 2026-07-27 — the probe pages mark `data-probe-ready` on `body` once their fixture state has rendered, and the README documents the waiting capture command (MUNIDESK-558). VERIFIED this wave: the documented command captured both rendered fixture states at 1100×720 on the first attempt, and the frontend suite passed (572 tests, 37 files).
- DONE 2026-07-27 — the installed Linux bundle's ASR runtime rpath matches the packaged layout (`$ORIGIN/../lib/muniment/asr-runtime` in `src-tauri/build.rs`), so the failing desktop-linux QA job is green again (MUNIDESK-561).
- VERIFIED 2026-07-27 (earlier wave) — the documented probe capture rendered both fixture states per spec at 1100×720 with no new UI defect, the frontend suite passed (572 tests, 37 files), and the core Rust suite passed in full. The planning clone cannot compile the `src-tauri` crate itself (no ALSA headers for `alsa-sys`); the desktop-ci VM builds remain the gate for that crate.
- VERIFIED 2026-07-27 (this wave) — the probe capture rendered both fixture states per spec at 1100×720 with no new UI defect, and the frontend suite passed (572 tests, 37 files). One core Rust test flaked under full-suite load: the 4 MiB byte-window streaming test in `core/tests/attach_linux_session.rs` hit `UnexpectedEof` when its 2-second session deadline fired, and passed five of five in isolation. A deflake ticket is filed (this wave, code-health lens).
- VERIFIED 2026-07-27 (this wave) — the probe capture rendered both fixture states per spec at 1100×720 with no new UI defect, and the frontend suite passed (572 tests, 37 files). The full core Rust suite passed, including the 4 MiB byte-window attach test under full-suite load this time. The open deflake ticket stands, because that pass is timing-sensitive.
- VERIFIED 2026-07-27 (this wave) — the documented probe capture rendered both fixture states per spec at 1100×720 from a clean clone, and no new UI defect appeared. The frontend suite passed (572 tests, 37 files). The core Rust suite passed in full (629 tests across 51 binaries), and `cargo clippy --all-targets` reported no warnings. The probe stub answers `chat_history` only, so the frontend switch must extend `test/probe/stub.js` to the thread commands in the same slice.
- VERIFIED 2026-07-27 (earlier wave) — the documented probe capture rendered both fixture states per spec at 1100×720 from a clean clone, including the new first-prompt thread title in the titlebar and the sidebar row. No new UI defect appeared. The frontend suite passed (581 passed, 24 skipped, across 38 files). The core Rust suite passed in full across 51 binaries. The probe stub still answers `chat_history` alone, and it has no `pending-permission` fixture, so the ask-card slice must add one.
- VERIFIED 2026-07-28 (earlier wave) — the documented probe capture rendered all three fixture states per spec at 1100×720, including the new `permission.html` page. The ask card shows the mono title, the exact path, and Allow beside Deny. No new UI defect appeared. The frontend suite passed (598 passed, 24 skipped, across 38 files). The core Rust suite passed in full across 52 test binaries.
- VERIFIED 2026-07-28 (this wave) — the documented probe capture rendered all five fixture states per spec at 1100×720, including the new `input.html` and `editor.html` pages. No new UI defect appeared. The frontend suite passed (600 passed, 24 skipped, across 38 files). The core Rust suite passed in full (637 tests across 51 binaries), and `cargo clippy --all-targets` reported no warnings.
- OBSERVED 2026-07-28 (planner, first-run lens) — the probe harness covers signed-in states alone, so the first-run onboarding surface never renders at planning time. This wave the planner rendered it from a throwaway stub with `home_status.configured` false and the required model downloading. The surface matches §15: the eyebrow, the `Choose your Muniment Home` heading, the Local AI progress card, the Home path row, the four folder chips, and the primary Confirm action. No defect appeared, so no ticket is filed. A committed onboarding fixture is worth a later slice, and it must not race another slice that edits `test/probe/stub.js`.
- VERIFIED 2026-07-28 (this wave) — the documented probe capture rendered the empty and restored-history fixtures per spec at 1100×720 from a clean clone, now including the sidebar recents rows. The frontend suite passed (618 passed, 24 skipped, across 39 files). The core Rust suite passed in full (637 tests across 52 binaries). Two list defects came out of a throwaway 12-thread render and are filed above. `npm run probe` prints four fixture URLs and omits `permission.html`, so the ask-card slice corrects the script it uses.
- VERIFIED 2026-07-28 (this wave) — the documented probe capture rendered all five fixture states per spec at 1100×720 from a clean clone, and `npm run probe` now prints all five URLs. The artifact rail and the profile popover were captured again and match §2.1. The frontend suite passed (632 passed, 24 skipped, across 39 files). The core Rust suite passed in full (639 tests across 51 binaries). One sidebar defect came out of a scripted new-thread-plus-prompt run through the bundle and is filed above.
- VERIFIED 2026-07-28 (earlier wave) — the documented probe capture rendered both fixture states per spec at 1100×720, and the restored-history fixture again at the 960×640 window minimum, where nothing overflows. No new UI defect appeared. The frontend suite passed (581 passed, 24 skipped, across 38 files). The core Rust suite passed in full (636 tests across 51 binaries). Both selected frontend slices extend `test/probe/stub.js`, so they land in sequence rather than side by side.
- VERIFIED 2026-07-28 (earlier wave) — the documented probe capture rendered the restored-history, permission, input, and editor fixtures per spec at 1100×720 from a clean clone. The sidebar recents list, the current-thread record, both ask cards, and the provenance register match spec. The frontend suite passed (634 passed, 24 skipped, across 39 files). The core Rust suite passed in full (639 tests across 51 binaries). One copy defect came out of the render and is recorded above.
- VERIFIED 2026-07-28 (earlier wave) — the documented probe capture rendered all five fixture states per spec at 1100×720 from a clean clone, including the landed inline-rename titlebar control. No new UI defect appeared beyond the recorded `local · durable` literal. The frontend suite passed (645 passed, 24 skipped, across 39 files). The core Rust suite passed in full (639 tests across 51 binaries).
- VERIFIED 2026-07-29 (this wave) — the documented probe capture rendered all five fixture states per spec at 1100×720 from a clean clone. The titlebar now carries the session name and the artifact toggle alone. The frontend suite passed (645 passed, 24 skipped, across 39 files). The core Rust suite passed in full (639 tests across 52 binaries), and `cargo clippy --all-targets` on `muniment-core` reported no warnings. The sidebar defect recorded above came out of a scripted read of the rendered rows.
- CORRECTED 2026-07-29 (planner) — the open deflake ticket noted above has landed. MUNIDESK-569 raised that test's session deadline, and `src-tauri/core/tests/attach_linux_session.rs` now passes 30 seconds where the flaking run passed 2. The full core suite passed again this wave under load.
- MEASURED 2026-07-29 (planner, launch render lens) — the shell is not the cost of opening a long thread. `chat-controller.openThread` pages `chat_thread_open` up to 100 times at 100 entries a page, and it renders nothing until the last page arrives. Against a throwaway stub that answers instantly, the built bundle reached a full render in 176ms for 100 runs, 366ms for 500 runs, and 1,154ms for 2,000 runs at 1100×720. DOM cost is about 0.5ms per turn and linear, so the launch cost lives in the journal read the 2026-07-28 note above describes, not in the shell. No windowing slice is selected.
- VERIFIED 2026-07-29 (this wave) — the documented probe capture rendered the empty, restored-history, and permission fixtures per spec at 1100×720 from a clean clone. The frontend suite passed (650 passed, 24 skipped, across 39 files). The core Rust suite passed in full (639 tests across 51 binaries).
- DONE 2026-07-29 — that fixture landed (MUNIDESK-632). `test/probe/onboarding.html` renders first-run setup during the required-model download, `test/probe/stub.js` answers `home_status` and `required_model_acquisition_status` for it with values the commands actually return, `fixtureRendered` gained its own onboarding branch, and `npm run probe` prints six URLs. Planner render 2026-07-29 of the built bundle at 1100x720 confirmed the eyebrow, the `Choose your Muniment Home` heading, the Local AI card at `Downloading` with `564 MB of 2.0 GB`, the Home path row, the four folder chips, and the primary Confirm action.
- VERIFIED 2026-07-29 (this wave) — the documented probe capture rendered the empty, restored-history, and permission fixtures per spec at 1100×720 from a clean clone. No new UI defect appeared. The frontend suite passed (650 passed, 24 skipped, across 39 files), and the core Rust suite passed in full.
- MEASURED 2026-07-29 (planner, re-dissecting the stuck QA ticket for `wry.0_55_1.linux.installed_nightly_model-ready_onboarding`) — `test/e2e/specs/onboarding.spec.js` carries a second, certain staleness beyond the reported assertion. It clicks `onboarding-confirm` and then waits for `button=Sign in`. `confirmHome` (`src/lib/Onboarding.svelte:57`) moves an unconfigured Home to the import-choice screen, and `src/App.svelte:655` renders the sign-in lockup only once onboarding is complete, so that wait can never pass. MUNIDESK-635 landed the same three-line pass-through in the sibling `real-sign-in.spec.js` hours earlier. The reported failure is the first wait instead, and the model-ready run is the first launch of the freshly installed package (`test/e2e/runner/linux.sh:184`), which also starts the required-model download in `src-tauri/src/main.rs` setup. `wdio.conf.js:36` gives that wait 30 seconds while mocha allows 180. DONE 2026-07-29 — both slices landed. MUNIDESK-638 clicks through the shipped import step before waiting for `Sign in`. MUNIDESK-639 bounds the first-render wait and fails with a text diagnostic naming the parked onboarding state, with the harness assertion added in `test/desktop-e2e-harness.test.js`.
- VERIFIED 2026-07-29 (this wave) — the documented probe capture rendered all six fixture states per spec at 1100x720 from a clean clone, including the newly committed onboarding fixture. The frontend suite passed (651 passed, 24 skipped, across 39 files). The core Rust suite passed in full (640 tests across 53 binaries). The two defects recorded above came out of scripted drives through the built bundle rather than from static reads.
- MEASURED 2026-07-29 (planner, second re-dissection of the stuck Linux onboarding ticket) — the blank first render is a product defect rather than a spec staleness. `default_home` (`src-tauri/src/home.rs:77`) turns a failed `document_dir()` into the error `The Documents folder is unavailable.`, `home_status` (`src-tauri/src/home.rs:85`) calls it on every run with no configured Home, and the `loadOnboarding` catch (`src/lib/Onboarding.svelte:21`) parks the surface in `load-error`. That branch renders `Onboarding could not start.` with `Try again` and no picker, which the planner confirmed by rendering the state through the built bundle at 1100x720. Tauri resolves `document_dir()` through `dirs::document_dir()` (`tauri-2.11.5/src/path/desktop.rs:108`), and `dirs-sys 0.5.0` reads `$XDG_CONFIG_HOME/user-dirs.dirs` alone (`src/lib.rs:114-123`). A planner probe built against `dirs 6.0.0` returned `None` for `document_dir()` under a fresh `XDG_CONFIG_HOME` and a real path for `home_dir()`. `test/e2e/runner/linux.sh:182` exports exactly that fresh state, so the installed job meets this path on every run. Any minimal Linux install with no registered Documents folder meets it too.
- DESIGN CALL 2026-07-29 (planner, the fallback default Home) — the runner keeps its fresh XDG state, and the product answers with a usable default. Seeding `user-dirs.dirs` in the runner would hide a fail-closed path that harness-spec §15 forbids. The freedesktop `xdg-user-dir` tool prints `$HOME` for a missing key, so the home directory is the conventional fallback, and the `dirs` crate tracks the same request as xdg-rs/dirs issue 56. Core `create_visible_directory` (`src-tauri/core/src/home.rs:1218`) creates one directory level, so the default must sit directly inside a directory that already exists. The fallback is therefore `<home>/Documents/Muniment` when that parent exists and `<home>/Muniment` otherwise. DONE 2026-07-29 — both halves landed. MUNIDESK-660 added `choose_default_home` (`src-tauri/src/home.rs:76`), which returns `<home>/Documents/Muniment` when that parent exists and `<home>/Muniment` otherwise. MUNIDESK-661 gave the `load-error` branch a `Choose folder…` control beside `Try again`. Planner review 2026-07-30 confirmed the `is_dir` guard on the home directory and the picker in the error branch.
- DONE 2026-07-30 — the attach half of that fix landed (MUNIDESK-684). `resolve_attach_home` (`src-tauri/src/attach_service.rs:123`) routes the Linux attach Home through the same `choose_default_home` fallback the picker takes, so a missing Documents folder no longer maps to `persistence_failed` and the listener answers its peers. Planner review 2026-07-30 confirmed the shared helper and the test that resolves `<home>/Muniment` when the Documents folder is absent.
- DONE 2026-07-29 — the two dead runner variables are gone (MUNIDESK-662).
- VERIFIED 2026-07-29 (this wave, first-run correctness lens) — the documented probe capture rendered the empty, restored-history, onboarding, and permission fixtures per spec at 1100x720 from a clean clone. The frontend suite passed (651 passed, 24 skipped, across 39 files). The core Rust suite passed in full across 53 test binaries. The committed onboarding fixture answers a successful `home_status`, so the failing first-run state came from a throwaway stub that rejects it.
- MEASURED 2026-07-29 (planner, reproduced the reported Windows failure on Linux) — the stuck Windows ticket names one cause of four. `test/e2e/runner/windows.ps1:181` runs `npm test` inside the VM, and four blocks across two test files spawn `bash`. Removing `bash` from `PATH` on Linux reproduces the operator's file shape exactly: `Test Files 2 failed | 37 passed (39)`, with 69 failed tests. The failures sit in `cleanup failure accounting` (41 of its 42 tests), `desktop-ci payload extraction` (14 of 26), `macOS installed launch harness` (10 of 13), and the four `ensure-junit-report.sh` tests in `test/nightly-workflow.test.js`. Gating the named block alone leaves 28 failures, so `npm test` still exits non-zero and the runner still throws.
- DONE 2026-07-29 — all three Windows contract slices landed. Slices 1 and 2 gated the shell-spawning blocks in `test/desktop-e2e-harness.test.js` and `test/nightly-workflow.test.js`. Slice 3 added `test/posix-shell-gate.test.js` (MUNIDESK-679), which reads the suite source and fails when a new `bash` call site appears outside a guarded block, in the `signal-allowlist.test.js` idiom.
- SEQUENCED 2026-07-29 — the gate belongs on every block that spawns a POSIX shell rather than on Linux alone. `macos.sh` runs on the macOS VM, and `extract-artifacts.sh` plus `ensure-junit-report.sh` run on the Linux GitHub runner for all three lanes, so a Windows skip loses no real coverage. The fix splits by file. Slice 1 gates the three blocks in `test/desktop-e2e-harness.test.js`. Slice 2 gates the four `ensure-junit-report.sh` tests in `test/nightly-workflow.test.js`. A source-reading guard over new `bash` call sites follows both, in the `signal-allowlist.test.js` idiom.
- VERIFIED 2026-07-29 (this wave) — the documented probe capture rendered all six fixture states per spec at 1100x720 from a clean clone. No new UI defect appeared. The frontend suite passed (651 passed, 24 skipped, across 39 files).
- VERIFIED 2026-07-29 (this wave) — the documented probe capture rendered all six fixture states per spec at 1100x720 from a clean clone, plus the restored-history fixture at the 960x640 window minimum. No new UI defect appeared, and nothing overflows at the minimum. The frontend suite passed (661 passed, 25 skipped, across 40 files). The core Rust suite passed in full (640 tests across 52 binaries), `cargo clippy -p muniment-core --all-targets` reported no warnings, and the `muniment-cli` suite passed (19 tests). The built `muniment-cli` binary answered `--help`, `-h`, `--version`, and a bare invocation with the same unsupported-arguments line on stderr and exit code 1. CORRECTED 2026-07-30 — that last sentence is now stale. MUNIDESK-658 landed the help slice, and the planner reran the built binary this wave.

- VERIFIED 2026-07-30 (this wave) — the documented probe capture rendered the empty, restored-history, onboarding, and permission fixtures per spec at 1100x720 from a clean clone. The onboarding fixture now renders the Home picker alone, because the Local AI card left with the resident model, and `test/probe/stub.js` no longer answers `required_model_acquisition_status`. The frontend suite passed (661 passed, 25 skipped, across 41 files). The core Rust suite passed in full, and `cargo clippy -p muniment-core --all-targets` reported no warnings.
- VERIFIED 2026-07-30 (this wave) — the documented probe capture rendered the empty, restored-history, onboarding, approved-files, and permission fixtures per spec at 1100x720 from a clean clone. The new approved-files fixture reaches `Save approved files` on its own. The frontend suite passed (661 passed, 25 skipped, across 41 files). The core Rust suite passed in full across 46 test binaries. The landmark defect recorded above came out of a scripted structure read of the same rendered pages rather than from a screenshot.
- NOTED 2026-07-30 (planner) — `test/probe/README.md` lists five fixture pages and omits `approved-files.html`, which `npm run probe` does print. The next slice that edits `test/probe/` corrects the list. Planner read 2026-07-30 found a second staleness in the same file: it still describes the onboarding fixture as first-run setup during the required local AI model download, and that card left with the resident model.
- VERIFIED 2026-07-30 (this wave) — the documented probe capture rendered the empty, restored-history, onboarding, approved-files, and permission fixtures per spec at 1100x720 from a clean clone. The frontend suite passed (662 passed, 25 skipped, across 41 files). The core Rust suite passed in full (569 tests across 46 binaries), and the `muniment-attach`, `muniment-cli`, and `muniment-acp` suites passed together.
- VERIFIED 2026-07-30 (this wave, error-handling lens) — scripted probes rejected `chat_thread_summaries`, `chat_thread_open`, and `chat_current_thread` in turn against the built bundle. The first two park the shell on the `Conversation history could not be restored.` alert beside its retry control, with the sidebar list still correct in the second case. A rejected `chat_current_thread` renders the whole restored thread and loses the current-row mark alone. No uncaught error reached the console in any of the three.
- OBSERVED 2026-07-30 (planner, copy lens) — an error message and its recovery control say the same words twice. Six `onHistoryError` strings in `src/lib/chat-controller.js` end with `Try again.`, and `src/App.svelte:689` renders every one of them beside a `Try again` button. `errorState` (`src/lib/auth-state.js:22`) and the resume error (`chat-controller.js:347`) repeat the shape beside their own controls. `AccessPanel.svelte:152` and the `Reply failed.` record at `App.svelte:715` are the counterexamples, and `submitError` and `cancelError` correctly keep the instruction because no control sits beside them. The rule worth writing into `DESIGN.md` is that the message names the failure and the control names the recovery. SELECTED 2026-07-30 (this wave) — that rule, now that both copy-bearing slices have landed. Planner render 2026-07-30 of the built bundle with a rejected `chat_thread_summaries` shows `Conversation history could not be restored. Try again.` eight pixels from a `Try again` button. Current practice agrees: NN/g's error-message guidance asks for short, non-redundant text beside a control that states the action it takes.
- DONE 2026-07-30 — that rule landed (MUNIDESK-696). `DESIGN.md` carries the line `An error message names the failure, and the control beside it names the recovery.` Six `onHistoryError` strings in `src/lib/chat-controller.js`, `errorState` in `src/lib/auth-state.js`, and the resume error each drop the duplicated instruction, and `submitError` and `cancelError` keep theirs because no control sits beside them.
- OBSERVED 2026-07-30 (planner) — the import dead end recorded above escaped every layer for a fourth reason in the series. `test/probe/` has no fixture past the Home picker, so planning-time rendering never reached the import screens, and `src/App.test.js` asserts the wall as intended behavior. This wave the planner reached it by driving the built bundle through the flow from a throwaway stub. A committed import fixture belongs in the slice that fixes the flow, because both edit the same screens.
- VERIFIED 2026-07-30 (this wave) — the documented probe capture rendered the restored-history, onboarding, and approved-files fixtures per spec at 1100x720 from a clean clone. The approved-files screen now shows `Home location` and `Back to archive review`. The frontend suite passed (666 passed, 25 skipped, across 41 files). The Rust suites passed package by package. The copy defect recorded above came from a throwaway stub that rejects `chat_thread_summaries`, and the key-order defect recorded above came from running core beside the adapter in one cargo invocation.
- VERIFIED 2026-07-30 (this wave) — the documented probe capture rendered the empty, restored-history, onboarding, approved-files, and permission fixtures per spec at 1100x720 from a clean clone. No new defect appeared in a static read. The frontend suite passed (666 passed, 25 skipped, across 41 files). One cargo invocation over `muniment-core`, `muniment-attach`, `muniment-cli`, and `muniment-acp` passed 660 tests across 55 binaries, so the key-order pin holds where the pre-fix run failed. Both thread-surface defects recorded above came from driving replaced reply text through the built bundle rather than from a static read.
- VERIFIED 2026-07-30 (this wave) — the documented probe capture rendered the empty, restored-history, onboarding, and permission fixtures per spec at 1100x720 from a clean clone. The streamed run, the expanded receipt, and the open artifact rail each render per spec, and nothing overflows at the 960x640 minimum. The frontend suite passed (668 passed, 25 skipped, across 42 files). One cargo invocation over `muniment-core`, `muniment-attach`, `muniment-cli`, and `muniment-acp` passed 660 tests across 55 binaries, so the key-order pin still holds. The two thread-surface defects recorded above came from driving a live run and a failed tool through the built bundle rather than from a static read.
- CORRECTED 2026-07-31 (planner) — the `test/probe/README.md` staleness recorded above is gone. The file now lists all seven fixture pages, including `approved-files.html`, and it no longer describes the onboarding fixture as a required local AI model download.
- VERIFIED 2026-07-31 (this wave) — the documented probe capture rendered the empty, restored-history, onboarding, approved-files, and permission fixtures per spec at 1100x720 from a clean clone. No new defect appeared in a static read. The frontend suite passed (703 passed, 25 skipped, across 43 files). One cargo invocation over `muniment-core`, `muniment-attach`, `muniment-cli`, and `muniment-acp` passed with no failure, so the key-order pin still holds. The raw-Markdown measurement recorded above came from driving a live streamed reply through the built bundle to its terminal event rather than from a static read.
- VERIFIED 2026-07-31 (this wave) — the documented probe capture rendered the restored-history and Markdown fixtures per spec at 1100x720 from a clean clone, and the Markdown fixture again at the 960x640 minimum. The rendered reply carries a real heading, list, inline code, fenced block, and table, and no page error reached the console. The frontend suite passed (713 passed, 25 skipped, across 44 files). One cargo invocation over `muniment-core`, `muniment-attach`, `muniment-cli`, and `muniment-acp` passed 664 tests across 55 binaries. The link measurement recorded above came from reading the rendered anchor against the desktop's registered plugins, and the sidebar correction came from a scripted render with relabelled time rows.
- MEASURED 2026-07-31 (planner, ran the documented capture from a clean clone) — the restored-history fixture never becomes ready, so the documented command hangs on it. `fixtureRendered()` (`test/probe/stub.js:155`) asks whether `workspace.textContent` contains each run's exact text, and the second run carries `Subject: Lease renewal notice\n\nThis is a reminder that`. `src/App.svelte:728` now renders an interrupted run through `AssistantMarkdown`, which emits two `p` elements, so `textContent` reads `...noticeThis is a reminder...` and the substring check fails forever. The `MutationObserver` never rescues a logic mismatch. The Markdown component caused this, because that reply used to render as one `p` with `white-space: pre-wrap`. `markdown.html` and `onboarding.html` still capture normally.
- MEASURED 2026-07-31 (planner, page-performance lens over the same captures) — a probe capture can also shoot the wrong type. `markReadyAfterFixtureRender` (`test/probe/stub.js:178`) sets `data-probe-ready` on the DOM check alone and waits for no font, and all three `@font-face` rules in `src/styles/tokens.css` declare `font-display: swap` at lines 15, 24, and 34. Two captures of the same page in one session against the same server disagreed. One rendered Schibsted Grotesk and Commit Mono, the other rendered the fallback stack, where every mono chip changes width and the user-bubble left edge moves 16 pixels. Planning-time rendering is the layer that catches geometry, so a capture in the wrong type invites a withdrawn observation.
- SELECTED 2026-07-31 (this wave) — both harness fixes together, because both live in the same function of `test/probe/stub.js`.
- DONE 2026-07-31 — both landed (MUNIDESK-715). `fixtureRendered` compares the rendered text with whitespace removed, so a reply split across two paragraphs still matches, and `markProbeReady` awaits `document.fonts.ready` before it sets `data-probe-ready`. `test/probe-harness.test.js` guards the single ready-marker write. Planner run 2026-07-31 of the documented capture command took all eight fixtures in one pass with no hang.
- MEASURED 2026-07-31 (planner, page-performance lens over the built bundle) — asset weight is not worth a slice. The frontend emits one 236,839-byte script, one 35,500-byte stylesheet, and 154,444 bytes of webfont. `marked` and DOMPurify account for roughly 55,000 bytes of that script, and they load at launch although Markdown renders only after a terminal run event. Tauri serves those bytes from local disk, so a dynamic import would trade a few milliseconds of parse time for an async boundary in a component that is synchronous today. No code-splitting slice is selected.
- VERIFIED 2026-07-31 (this wave) — the documented probe capture rendered the Markdown and onboarding fixtures per spec at 1100x720 from a clean clone, and the restored-history fixture rendered correctly once the hung readiness check was worked around. The frontend suite passed (717 passed, 25 skipped, across 45 files). One cargo invocation over `muniment-core`, `muniment-attach`, `muniment-cli`, and `muniment-acp` passed 664 tests with no failure across 53 binaries plus two doc-test suites, and `cargo clippy -p muniment-core --all-targets` reported no warnings. The two harness defects recorded above came from running the documented command itself rather than from a static read.
- VERIFIED 2026-07-31 (this wave) — the documented probe capture took all eight fixtures at 1100x720 from a clean clone with no hang, and every page rendered per spec. The frontend suite passed (725 passed, 25 skipped, across 47 files). One cargo invocation over `muniment-core`, `muniment-attach`, `muniment-cli`, and `muniment-acp` passed 673 tests across 56 binaries with no failure. A scripted structure read of all eight rendered pages found the named landmarks, the expected page heading, no console error, and no sideways overflow.
- OBSERVED 2026-07-31 (planner, same structure read) — the composer carries no persistent visible label. `src/App.svelte:841` gives the textarea a `placeholder` and no visible label, so its prompt leaves the screen as soon as the user types. Every other named control in the shell carries an `aria-label` or a heading. Not selected this wave, because three slices already fill it.
- MEASURED 2026-07-31 (planner, read every text entry under `src/`) — the composer is the one unnamed entry in the app. The rename field carries `Thread name` (`src/App.svelte:663`). Both permission gate fields carry the request title (`:756` and `:766`). The import consent control sits inside a `<label>` (`src/lib/Onboarding.svelte:152`). The composer textarea alone carries neither `aria-label`, nor `aria-labelledby`, nor a `<label>`. Its placeholder also doubles as a status string. It reads `Resuming interrupted reply…` while a run resumes, so the control's only name changes with run state. Current guidance is explicit that a placeholder is not a label, because it leaves the screen on the first keystroke and reaches assistive technology unreliably. Sources: `w3.org/WAI/tutorials/forms/instructions/` and `webaim.org/articles/label-name/`.
- SELECTED 2026-07-31 (the next wave) — that name, its `aria-describedby` tie to the composer hint line, and a source-reading lint in the `text-wrap.test.js` idiom. Nothing changes on screen, because `src/styles/base.css:73` already ships the `.visually-hidden` rule the announcement regions use. Three slices fill this wave.
- SELECTED 2026-07-31 (this wave) — that name, now that the wave has passed. Planner read 2026-07-31 confirmed the gap again. The composer textarea (`src/App.svelte:841`) carries a placeholder alone, and the hint line beside it (`src/App.svelte:853`) is an unnamed `span` with no `id`. The other four text entries under `src/` are named already: the rename input at `:663`, both permission fields at `:756` and `:766`, and the import consent checkbox inside its wrapping `<label>` (`src/lib/Onboarding.svelte:152`). Current guidance pairs the accessible name with `aria-describedby` for the help text, so the hint reaches the user after the name rather than instead of it. Sources: `developer.mozilla.org/en-US/docs/Web/Accessibility/ARIA/Reference/Attributes/aria-label` and `w3.org/WAI/tutorials/forms/instructions/`.
- VERIFIED 2026-07-31 (this wave) — the documented probe capture took all eight fixtures at 1100x720 from a clean clone. Nothing hung, and every page rendered per spec. A scripted structure read of all eight pages found the expected `h1`, no console error, and no sideways overflow. Every `.thread-list li` row measured 36.14px. The frontend suite passed (725 passed, 25 skipped, across 47 files). One cargo invocation over `muniment-core`, `muniment-attach`, `muniment-cli`, and `muniment-acp` passed 681 tests across 54 binaries with no failure. `cargo clippy -p muniment-core --all-targets` reported no warning. The glob re-export defect recorded above came from running that same four-package invocation as a check rather than as a test.
- MEASURED 2026-07-31 (planner, ran `npm test` beside a cargo build) — the POSIX shell gate walks build output and dies on it. `npm test` reported `1 failed | 46 passed` with `Error: ENOENT: no such file or directory, scandir 'src-tauri/target/debug/deps/rustcxJtEup'`. `testFiles` (`test/posix-shell-gate.test.js:20`) reads every directory under the repository root and skips only `node_modules`, `.git/`, `editor-extension/`, and `test/e2e/`. It therefore descends into `src-tauri/target/`, which held 475 directories and 5,665 files in this clone, and a rustc temporary directory vanished between the parent read and the child read. No `.test.js` file lives under any build output directory, so the walk reads thousands of entries it can never use. The four style lints avoid this by starting at `src` (`src/styles/class-usage.test.js:54`, `signal-allowlist.test.js:78`, `shape-scale.test.js:54`, `type-scale.test.js:44`). The smoke job runs `npm test` before the cargo steps on the self-hosted runner (`.github/workflows/ci.yml:129`), and that runner keeps its workspace between runs, so the walk reads a populated `src-tauri/target/` there too.
- SELECTED 2026-07-31 (this wave) — that exclusion. The gate's landed assertion that every `ALLOWED` key still appears is what keeps an over-broad exclusion honest, so it stays.
- VERIFIED 2026-07-31 (this wave) — the documented probe capture took all eight fixtures at 1100x720 from a clean clone with no hang, and every page rendered per spec. The restored-history, Markdown, empty, and approved-files fixtures each match their section above. The core Rust suite passed in full through `cargo test --manifest-path src-tauri/core/Cargo.toml --locked`. The frontend suite reported 727 passed, 25 skipped, and the one failure recorded above.

## Stable release and distribution
- DONE — rolling nightly one pinned SHA across Linux, signed Windows, unsigned macOS; owner-triggered SemVer promotion copies exact green artifacts and labels unsigned macOS.
- DONE groundwork — promotion hashes MSI, generates `Muniment.Muniment` WinGet manifest, and opens a draft fork PR. Fork/token setup, publication, Homebrew, and Apple signing remain owner-gated. No monetization or promotional surface is implied.
- DONE 2026-07-24 — macOS Developer ID signing, notarization, and stapling are pre-staged in the nightly release path behind the same desktop-ci env-injection seam Windows signing uses (`.github/build-macos-app.mjs`, `.github/lib/macos-signing.mjs`): with no Apple credentials the build stays a clean unsigned no-op, a partial credential set fails fast naming only the missing variables, and `docs/macos-signing.md` records the six vault keys plus the codesign/notarize/staple/Gatekeeper verification checklist. Apple enrollment Y5DUNHQA74 is still in review; switching signing on is secrets-only, and public download/install docs and promotion stay owner-gated.
- MEASURED 2026-07-31 (planner, built the frontend and read the emitted assets) — the shipped bundle carries third-party code and names none of it. `dist/assets/index-*.js` is 236,839 bytes and contains `marked` 18.0.7 (MIT), `dompurify` 3.4.12 (MPL-2.0 OR Apache-2.0), the Svelte 5.56.4 runtime (MIT), `@tauri-apps/api` 2.11.1, and the dialog, global-shortcut, and opener plugins (MIT OR Apache-2.0). A grep for `Copyright`, `@license`, and `SPDX` over that file and over the 35,500-byte stylesheet returns zero matches, because the minifier drops every legal comment. `THIRD_PARTY_NOTICES.md` records derived source and native runtimes alone, so it names no bundled package. The three webfonts have the same gap. `SchibstedGrotesk-latin.woff2`, `SchibstedGrotesk-latin-ext.woff2`, and `CommitMono-VF.woff2` all reach `dist/assets/`, both families are SIL OFL 1.1, and neither `src/fonts/SchibstedGrotesk-OFL.txt` nor `src/fonts/CommitMono-LICENSE.txt` is referenced by the build, so no license text ships beside the binaries. OFL 1.1 clause 2 and the MIT text both require the notice to travel with the copy.
- SELECTED 2026-07-31 (this wave) — the record and the guard that keeps it complete. Shipping the notice inside the installed package follows as its own slice, because it touches `tauri.conf.json` bundling and needs a check on each of the three platforms.
- DONE 2026-07-31 — that record landed (MUNIDESK-716). `THIRD_PARTY_NOTICES.md` names every bundled frontend package at its resolved version and both webfont families, and `test/third-party-notices.test.js` fails when a bundled package is missing or recorded at the wrong version.
- MEASURED 2026-07-31 (planner, read the three platform bundle configs against the built assets) — the notice ships and the license texts it cites do not. `tauri.linux.conf.json`, `tauri.macos.conf.json`, and `tauri.windows.conf.json` each map `../THIRD_PARTY_NOTICES.md` into `third-party-notices/`, so the record itself reaches the installed package. Neither `src/fonts/SchibstedGrotesk-OFL.txt` nor `src/fonts/CommitMono-LICENSE.txt` appears in any config, and `npm run build` copies the three `.woff2` files into `dist/assets/` alone. The shipped notice therefore points at repository paths that the package does not hold. OFL 1.1 clause 2 requires the copyright notice and the license to travel with each copy of the font, as stand-alone text files or as readable metadata.
- SELECTED 2026-07-31 (the next wave) — shipping both license texts beside the fonts, with a config-reading test that keeps the pair complete. Naming the Rust crates compiled into the desktop binary stays unselected, because a generated crate inventory is its own slice.
- SELECTED 2026-07-31 (this wave) — that slice, now that the wave has passed. Planner read 2026-07-31 confirmed the gap again: `THIRD_PARTY_NOTICES.md:21` cites `src/fonts/SchibstedGrotesk-OFL.txt` and `src/fonts/CommitMono-LICENSE.txt`, and neither path appears in any of the three `bundle.resources` maps. The record therefore ships pointing at two files the package does not hold.
- DONE 2026-07-31 — that slice landed (MUNIDESK-726). `tauri.linux.conf.json`, `tauri.macos.conf.json`, and `tauri.windows.conf.json` each map both license texts into `third-party-notices/` beside the record. Planner read 2026-07-31 confirmed the pair in all three configs. Naming the Rust crates compiled into the desktop binary stays unselected, because a generated crate inventory is its own slice.

## Phase 4+ — Org surface (§9 items 16–19)
- Remote MCP consumption, local stdio allowlist, capability install flow, artifact side panel, and projects with redaction (`output withheld · connection not granted`).

## Runtime security posture
- OWNER PICKED 2026-07-28 — two candidates from the 2026-07-21 operator competitive review of Odysseus open this lane. Both start as documents, and both cover the runtime half that this repository owns.
- DONE 2026-07-28 — ADR 0018, the untrusted-content boundary, is accepted (MUNIDESK-614). It names one wrapper contract (`ChatMessage::untrusted_json`, a user-role message carrying one fixed header plus the JSON value), one enforcement point in `src-tauri/core/src/llama.rs`, the surface inventory, and the §16.2 static-base policy line as a proposal rather than a prompt change.
- DONE 2026-07-28 — that ADR's first implementation slice landed (MUNIDESK-617): `ChatMessage::untrusted_json` is the single wrapper in `src-tauri/core/src/llama.rs`, and all four resident-model requests build their external content through it. Planner check 2026-07-29 confirmed the four call sites and that no divergent preamble remains. The type boundary plus the construction-site lint is slice 2, and the MUNIQA injection suite follows both.
- DONE 2026-07-29 — that slice landed (MUNIDESK-631). `ChatMessage`'s two fields and its `system`/`user` constructors are private to `src-tauri/core/src/llama.rs`, so no caller outside that module can skip the wrapper, and `src-tauri/core/tests/resident_message_boundary.rs` reads the source and fails when a raw construction appears outside the five reviewed sites. `src-tauri/core/tests/llama_server.rs` builds its fixtures through the public request path instead. Planner review 2026-07-29 confirmed the private fields, the five-site allowlist, and the missing-site assertion that keeps the lint honest when a site is deleted. The MUNIQA injection suite still follows.
- MEASURED 2026-07-28 (planner grep over `src-tauri/core/src`) — no browser-control page read, no capability output, and no third-party skill text reaches a model in this repository today, so those surfaces enter the inventory as planned rather than built. The Pi lane sends the user prompt through `PromptCommand` (`core/src/sidecar/pi_chat.rs`) and Pi assembles its own context, so the resident model is this repository's model-context boundary.
- DONE 2026-07-28 — `THREAT_MODEL.md`, the runtime half of the trust boundary, landed (MUNIDESK-615). It states the boundary once, tables roles by capabilities over harness-spec §3, matrixes the per-surface capabilities, and answers for each loopback what identity it trusts and why a user cannot mint it. The named loopbacks are the attach socket (`core/src/attach/linux.rs`), the sign-in redirect catcher (`core/src/auth/loopback.rs`), the browser-control relay (§6.8), and the ADR 0012 runtime service, which is a decided direction rather than a built surface.
- MEASURED 2026-07-30 (planner, read `THREAT_MODEL.md` against the 2026-07-29 owner ruling and ADR 0022) — the runtime trust boundary document contradicts both. Its surface capability matrix still records the CLI and the editor extension as `Built` execution surfaces with no note that the owner deferred one indefinitely and retired the other. It carries no row for the ACP adapter, although the accepted ADR 0022 states that the editor-spawned adapter is a new local client identity under this document, that its claimed process name, ACP session ID, and `cwd` grant no authority, and that the runtime must name the adapter in approval and receipts. SELECTED 2026-07-30 (this wave) — that refresh. It is documentation alone, and it lands before the adapter code so the boundary record never trails the process it describes.
- DONE 2026-07-30 — that refresh landed (MUNIDESK-688). The surface matrix marks the CLI deferred and the editor extension retired, and it carries an ACP adapter row that records the unbuilt state and the zero-authority claim over the process name, the session ID, and the `cwd`.
- MEASURED 2026-07-30 (planner, read the shipped approval path end to end) — the desktop's only attach approval prompt names two surfaces that no longer exist as products. It asks `Allow the CLI or VS Code to connect to this Muniment desktop session?` (`src/App.svelte:487`). The owner deferred the CLI indefinitely and retired the editor extension on 2026-07-29. The prompt also names neither the program that asked nor its version, although the session runner already reads both from the Hello and binds them (`src-tauri/core/src/attach/linux.rs:904`), and `attach_service.rs:393` writes the same claimed kind into run provenance. `ApprovalWaiter::wait` (`src-tauri/core/src/attach/linux.rs:335`) receives the opaque `PairingChallenge` alone, and `attach-pairing-requested` emits that challenge string alone, so the desktop cannot name the caller even though it knows the claim. ADR 0009 requires an explicit, visible approval naming the companion kind.
- SELECTED 2026-07-30 (this wave) — carrying the claimed kind and version into the approval callback, the emitted event, and the prompt copy. The claimed values are client-chosen text under `THREAT_MODEL.md`, so the prompt bounds their length, strips control characters, and marks them as claimed rather than verified. Naming the requested workspace scopes stays parked: approval runs before `onboard_workspace`, so the desktop does not know the scope yet, and moving the order is a protocol change.
- DONE 2026-07-30 — that slice landed (MUNIDESK-691). The approval callback and the `attach-pairing-requested` event carry the claimed kind and version, and the prompt names the program that asked instead of the CLI and VS Code. Planner check 2026-07-30 confirmed the length bound, the control-character strip, and the claimed-not-verified wording.
- The standing policy line for the agent system prompt stays a proposal inside ADR 0018. harness-spec §16.1 rule 4 gates prompt text on review and evaluation, and §16.2 records base v0 as owner-accepted verbatim.
- The muniment-cloud control-plane sibling document belongs to that repository. Any public security page derived from either document stays owner-gated.

## Standing gates
- Code PRs: structure smoke, frontend/Rust tests, applicable path-scoped checks or desktop builds. Markdown-only PRs gate on structure smoke; pushes to `main` run smoke only.
- Build desktop targets only unless an accepted ADR changes companion checks.
- Every pull request and every push to `main` runs the gitleaks secret scan (`.github/workflows/secret-scan.yml`). DONE 2026-07-28 — that scan reads the pull request's own commits (MUNIDESK-607), so a secret added and then removed inside one branch no longer merges unseen.
- Distribution accounts, marketplace/store publishing, production launch, and publicity remain owner-gated.
