# Speech model installation

Muniment downloads its speech model only after a user presses Voice and selects Install.

The install card shows the download size, source repository, licenses, and required free disk space.
The card reports the install state without a byte-level progress bar.

After installation, press Voice again to start dictation.
